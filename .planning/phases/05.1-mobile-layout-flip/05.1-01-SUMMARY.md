---
phase: 05.1-mobile-layout-flip
plan: 01
subsystem: ui

tags: [zustand, tailwind, react, responsive, mobile, sidebar]

# Dependency graph
requires:
  - phase: 04-responsive-shell
    provides: sidebar drawer pattern (being retired on mobile), safe-area insets, data-sidebar-trigger attribute
  - phase: 05-mobile-canvas
    provides: canvas grid + single-view + iOS inline video (preserved, will be reused inside gallery overlay in Plan 02)
provides:
  - galleryOpen state slice (boolean) + setGalleryOpen + toggleGallery actions in useWorkspaceStore
  - Sidebar rendered as in-flow full-width primary column below lg (no more drawer)
  - Sidebar rendered as 32rem fixed-width column at lg+ (desktop unchanged)
  - Clean foundation for Plan 02 (top bar with Gallery button + full-screen gallery overlay)
affects: [05.1-02-gallery-overlay-and-topbar, 06-mode-panels, 07-auxiliary-views-touch-ergonomics, 08-real-device-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "New gallery state slice (galleryOpen) is semantically independent of retiring sidebarOpen — lets Plan 02 consume it without touching desktop-visible sidebar state"
    - "Mobile-primary sidebar uses plain relative w-full with a single lg:w-[32rem] override — no drawer transforms, no backdrop role, no breakpoint-conditional template literal"

key-files:
  created: []
  modified:
    - src/stores/workspaceStore.ts
    - src/components/Sidebar.tsx

key-decisions:
  - "Chose Option B (new galleryOpen slice) over inverting sidebarOpen — CONTEXT.md preference for readability and so Plan 02 doesn't entangle with the retiring sidebarOpen drawer state"
  - "No matchMedia initializer for galleryOpen — CONTEXT 'Deferred Ideas' explicitly defers persistent gallery-open state; gallery always starts closed on load"
  - "Chose Option A (drop drawer pattern below lg, always render sidebar in flow) over keeping fixed positioning — cleaner diff, collapses the <aside> className to a single static string"
  - "Sidebar.tsx lost useEffect + useRef imports entirely — the sidebarOpen focus-return effect is dead code under the new model; Plan 02 will establish a new focus-return contract keyed to the Gallery close button"
  - "sidebarOpen slice preserved byte-for-byte in workspaceStore — Plan 02 will retire the WorkspaceLayout backdrop and remaining consumers incrementally"

patterns-established:
  - "Phase 5.1 breakpoint convention: mobile-primary layout is the plain Tailwind default, lg: overrides restore the desktop column — same mental model as Phase 5's p-0 lg:p-4 single-view padding"
  - "When a state slice is being retired but has lingering consumers, keep the slice intact and introduce a new independent slice for the new role rather than overloading the existing slice"

requirements-completed: [FLIP-01, FLIP-04]

# Metrics
duration: 1m 30s
completed: 2026-04-19
---

# Phase 5.1 Plan 01: Store and Sidebar Flip Summary

**galleryOpen state slice added; Sidebar flipped from mobile drawer to in-flow primary column while preserving the desktop 32rem fixed-width column exactly.**

## Performance

- **Duration:** 1m 30s
- **Started:** 2026-04-19T12:29:12Z
- **Completed:** 2026-04-19T12:30:42Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added `galleryOpen: boolean` (default `false`) state slice with `setGalleryOpen` and `toggleGallery` actions to `useWorkspaceStore` — foundation Plan 02 will consume
- Replaced Sidebar drawer classes (`fixed inset-y-0 z-40 w-[85vw] max-w-[360px] translate-x-*`) with in-flow classes (`relative w-full ... lg:w-[32rem]`); mobile sidebar now renders as the primary full-width column and desktop column is unchanged
- Removed dead `sidebarOpen` focus-return useEffect and the `useEffect`/`useRef` imports from `Sidebar.tsx` — Plan 02 will establish a new focus-return contract keyed to the Gallery close button
- Preserved every sidebar child panel (`BrandFacePanel`, `OnModelPanel`, `CatalogPanel`, `ColorwayPanel`, `DesignCopyPanel`, `VideoPanel`, `PromptPanel`, `GenerationControls`), account row, safe-area insets, language switcher, theme toggle, and sign-out button byte-for-byte
- Preserved the `sidebarOpen` slice + `getInitialSidebarOpen` initializer in `workspaceStore.ts` byte-for-byte so Plan 02 can retire the WorkspaceLayout backdrop and remaining consumers incrementally

## Task Commits

Each task was committed atomically:

1. **Task 1: Add galleryOpen slice to useWorkspaceStore** - `c99c15dc` (feat)
2. **Task 2: Flip Sidebar from mobile-drawer to mobile-primary-in-flow column** - `a4371959` (refactor)

**Plan metadata:** _(appended below after this file is committed)_

## Files Created/Modified

- `src/stores/workspaceStore.ts` - Added `galleryOpen` state slice (`boolean` + `setGalleryOpen` + `toggleGallery`) after the existing sidebar slice; preserved all other slices including `sidebarOpen` and `getInitialSidebarOpen`.
- `src/components/Sidebar.tsx` - Replaced drawer className template literal with static `relative w-full border-r border-border bg-background flex flex-col lg:w-[32rem]`; removed `sidebarOpen` subscription and focus-return `useEffect` + `useRef` imports; every child panel and safe-area inset preserved.

## Decisions Made

- **New `galleryOpen` slice over inverting `sidebarOpen`:** Option B in CONTEXT.md — cleaner semantics, zero coupling with the retiring drawer state.
- **No persistent gallery-open state:** Deferred per CONTEXT; gallery starts `false` on every load.
- **Option A for sidebar mobile flip:** Drop drawer pattern below `lg` entirely; the `<aside>` className collapses to a single static string.
- **`sidebarOpen` slice preserved in store:** Plan 02 retires the remaining consumers (WorkspaceLayout backdrop, hamburger trigger). Touching it now would bleed scope.
- **Removed `useEffect` + `useRef` imports entirely:** Plan 02 will add fresh focus-management for the Gallery close → Gallery trigger handoff; the prior focus-return effect was tied to a mobile open/close cycle that no longer exists.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 02 foundation ready:** `galleryOpen` slice is live; Sidebar is an in-flow primary view on mobile and an unchanged fixed column on desktop.
- **Intermediate state note:** At this commit the mobile viewport renders both Sidebar and Canvas stacked because `WorkspaceLayout` composition is untouched. This is a correct-but-not-shippable intermediate; Plan 02 adds the top bar + gallery overlay and removes the backdrop. The `sidebarOpen` default is `false` on mobile (from Phase 04-02's `getInitialSidebarOpen` matchMedia guard), so the WorkspaceLayout backdrop does not render on first load.
- **Regression locks preserved:**
  - LAYOUT-05 safe-area insets (`pt-[max(0.75rem,env(safe-area-inset-top))]` header, `pb-[max(0.75rem,env(safe-area-inset-bottom))]` account row) — both intact.
  - Desktop 32rem column — intact via `lg:w-[32rem]`.
  - Phase 5 canvas work (grid overlay, single-view fill, iOS `playsInline muted` video) — untouched.
- **Build green:** `npm run build` exits 0; TypeScript compiles after removing unused imports.

---
*Phase: 05.1-mobile-layout-flip*
*Completed: 2026-04-19*

## Self-Check: PASSED

- `.planning/phases/05.1-mobile-layout-flip/05.1-01-SUMMARY.md` — found
- `src/stores/workspaceStore.ts` — found (modified)
- `src/components/Sidebar.tsx` — found (modified)
- Commit `c99c15dc` (Task 1) — found
- Commit `a4371959` (Task 2) — found
