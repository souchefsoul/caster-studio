---
phase: 07-auxiliary-views-touch-ergonomics
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/PromptPanel.tsx
autonomous: true
requirements: [TOUCH-04]

must_haves:
  truths:
    - "Typing a long prompt in the shared PromptPanel grows the textarea vertically inside the scrollable sidebar instead of overflowing horizontally or clipping"
    - "The textarea renders at 16px font-size (text-base) so iOS Safari does not auto-zoom on focus"
    - "The textarea has a 3-row floor (min-h-[5rem]) before growth begins, matching Phase 6 Design Copy / Video textarea recipes"
    - "Desktop (lg+) rendering is preserved — the same textarea also grows on wider viewports without any new breakpoint gate"
  artifacts:
    - path: "src/components/PromptPanel.tsx"
      provides: "Shared prompt textarea used by On-Model, Catalog, Colorway modes — now auto-growing at 16px floor"
      contains: "field-sizing-content min-h-[5rem] text-base resize-none rounded-none"
      min_lines: 20
  key_links:
    - from: "src/components/PromptPanel.tsx"
      to: "Tailwind v4 field-sizing-content utility"
      via: "className string"
      pattern: "field-sizing-content"
    - from: "src/components/PromptPanel.tsx"
      to: "3-row SSR fallback floor"
      via: "rows={3} attribute on <textarea>"
      pattern: "rows=\\{3\\}"
---

<objective>
Apply the Phase 6 textarea triple-guard recipe (`field-sizing-content` + `min-h-[5rem]` + `text-base` + `rows={3}` + `resize-none`) to the shared PromptPanel textarea that feeds On-Model, Catalog, and Colorway modes.

Purpose: Close TOUCH-04 for the last remaining long-input textarea. Phase 6 already handled the mode-specific Design Copy (06-04) and Video (06-05) prompt textareas; PromptPanel is the shared textarea driving three modes and is the final place the pattern needs to land.

Output: 1 className mutation on the `<textarea>` element in `src/components/PromptPanel.tsx`. Zero changes to business logic (store bindings, handler callbacks).
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auxiliary-views-touch-ergonomics/07-CONTEXT.md
@.planning/phases/06-mode-panels-on-mobile/06-04-SUMMARY.md
@.planning/phases/06-mode-panels-on-mobile/06-05-SUMMARY.md

<interfaces>
<!-- Phase 6 reference recipe — Design Copy textarea final class string (06-04): -->
<!--   "w-full rounded-none border border-input bg-background px-3 py-2 font-mono text-base min-h-[5rem] field-sizing-content resize-none placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring" -->

<!-- Phase 6 reference recipe — Video prompt textarea final class string (06-05): -->
<!--   "w-full resize-none rounded-none border border-border bg-background px-3 py-2 text-base min-h-[5rem] field-sizing-content placeholder:text-muted-foreground focus:outline-none focus:ring-1 focus:ring-ring" -->

<!-- PromptPanel CURRENT class string (src/components/PromptPanel.tsx line 19): -->
<!--   "w-full resize-none rounded-none border border-input bg-background px-2.5 py-2 font-mono text-sm text-foreground placeholder:text-muted-foreground focus:border-ring focus:outline-none" -->
<!-- And rows={4} on line 15. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply field-sizing-content + min-h-[5rem] + text-base to PromptPanel textarea</name>
  <files>src/components/PromptPanel.tsx</files>

  <read_first>
    - src/components/PromptPanel.tsx (whole file, 24 lines)
    - src/components/DesignCopyPanel.tsx (line ~24 — reference recipe; DO NOT EDIT)
    - src/components/VideoPanel.tsx (line ~161 — reference recipe; DO NOT EDIT)
  </read_first>

  <action>
Modify `src/components/PromptPanel.tsx` exactly as follows. Preserve `<Label className="text-xs">` unchanged (label font stays small — TOUCH-02 rule applies to typed inputs only, not labels).

**Change 1: `rows` attribute (line 15).**
Current:
```tsx
          rows={4}
```
Change to:
```tsx
          rows={3}
```
Rationale: 3-row floor matches the Phase 6 recipe. `field-sizing-content` grows from there as content expands.

**Change 2: `<textarea>` className (line 19).**
Current:
```tsx
          className="w-full resize-none rounded-none border border-input bg-background px-2.5 py-2 font-mono text-sm text-foreground placeholder:text-muted-foreground focus:border-ring focus:outline-none"
```
Change to:
```tsx
          className="w-full rounded-none border border-input bg-background px-2.5 py-2 font-mono text-base min-h-[5rem] field-sizing-content resize-none text-foreground placeholder:text-muted-foreground focus:border-ring focus:outline-none"
```

**Delta explanation (for reviewer clarity — not for the codebase):**
- `text-sm` → `text-base` (14px → 16px) — iOS no-auto-zoom compliance
- Inserted `min-h-[5rem]` — 3-row Tailwind floor (80px @ 16px × 1.5 line-height + py-2 ≈ 88px rendered)
- Inserted `field-sizing-content` — Tailwind v4 utility mapping to CSS `field-sizing: content`; lets the textarea grow with its content natively
- Retained `resize-none` (moved after `field-sizing-content` in canonical order) — browser auto-grow replaces manual drag-resize; keeping both would let user-drag fight content-track
- Retained `font-mono` — PromptPanel is the only PromptPanel with mono type; preserve the Windows 95 flat prompt-console feel unchanged
- Class order normalized: layout (`w-full`) → chrome (`rounded-none border border-input bg-background`) → spacing (`px-2.5 py-2`) → typography (`font-mono text-base`) → sizing (`min-h-[5rem] field-sizing-content`) → resize (`resize-none`) → color (`text-foreground`) → placeholder (`placeholder:text-muted-foreground`) → focus (`focus:border-ring focus:outline-none`)

Do NOT change any other line. `<Label className="text-xs">` stays. Store subscriptions (`params`, `setParams`) stay. Component exports stay.

Commit message: `feat(07-02): auto-grow shared PromptPanel textarea (TOUCH-04) via field-sizing-content + 16px floor`
  </action>

  <verify>
    <automated>
# The new triple-guard is present, all three tokens on one textarea className
grep -c "field-sizing-content" src/components/PromptPanel.tsx  # expect 1
grep -c "min-h-\[5rem\]" src/components/PromptPanel.tsx  # expect 1
grep -c "font-mono text-base" src/components/PromptPanel.tsx  # expect 1
# rows={3} applied (legacy rows={4} removed)
grep -c "rows={3}" src/components/PromptPanel.tsx  # expect 1
grep -c "rows={4}" src/components/PromptPanel.tsx  # expect 0
# Old text-sm gone (on the textarea) — but Label stays text-xs
grep -c "font-mono text-sm" src/components/PromptPanel.tsx  # expect 0
grep -c "text-xs" src/components/PromptPanel.tsx  # expect 1 (the Label)
# resize-none still present
grep -c "resize-none" src/components/PromptPanel.tsx  # expect 1
# Store bindings untouched
grep -c "useWorkspaceStore" src/components/PromptPanel.tsx  # expect 1
grep -c "params.prompt" src/components/PromptPanel.tsx  # expect 1
grep -c "setParams({ prompt" src/components/PromptPanel.tsx  # expect 1
# Build passes
npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - grep `field-sizing-content` in `src/components/PromptPanel.tsx` reports exactly 1 match
    - grep `min-h-\[5rem\]` in `src/components/PromptPanel.tsx` reports exactly 1 match
    - grep `font-mono text-base` in `src/components/PromptPanel.tsx` reports exactly 1 match
    - grep `rows={3}` in `src/components/PromptPanel.tsx` reports exactly 1 match
    - grep `rows={4}` in `src/components/PromptPanel.tsx` reports 0 matches
    - grep `font-mono text-sm` in `src/components/PromptPanel.tsx` reports 0 matches (legacy 14px font gone from textarea)
    - grep `text-xs` in `src/components/PromptPanel.tsx` reports exactly 1 match (Label preserved at small size per TOUCH-02 selective rule)
    - grep `resize-none` in `src/components/PromptPanel.tsx` reports exactly 1 match
    - grep `useWorkspaceStore` in `src/components/PromptPanel.tsx` reports exactly 1 match (store binding preserved)
    - grep `params.prompt` in `src/components/PromptPanel.tsx` reports exactly 1 match
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Shared PromptPanel textarea auto-grows with content, has 3-row floor, renders at 16px, preserves font-mono typewriter aesthetic, and maintains all existing store bindings. TOUCH-04 satisfied for the last long-input textarea in the app.</done>
</task>

</tasks>

<verification>
```bash
# Full build
npm run build

# Phase 6 regression check — Design Copy + Video textareas still carry the recipe
grep -c "text-base min-h-\[5rem\] field-sizing-content" src/components/DesignCopyPanel.tsx  # expect 1
grep -c "text-base min-h-\[5rem\] field-sizing-content" src/components/VideoPanel.tsx  # expect 1
```
</verification>

<success_criteria>
- TOUCH-04 satisfied for the shared PromptPanel textarea used by On-Model, Catalog, Colorway modes.
- Triple-guard pattern (field-sizing-content + min-h-[5rem] + rows={3}) applied uniformly across all three Phase-6/7 long-input textareas (Design Copy, Video prompt, PromptPanel).
- iOS Safari auto-zoom prevention: 16px floor now applied to every user-typed input/textarea inside the mode-panel surface.
- Zero business-logic change, zero store-binding change.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auxiliary-views-touch-ergonomics/07-02-SUMMARY.md`
</output>
