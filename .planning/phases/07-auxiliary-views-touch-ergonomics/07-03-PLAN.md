---
phase: 07-auxiliary-views-touch-ergonomics
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified: []
autonomous: true
requirements: [TOUCH-03]

must_haves:
  truths:
    - "Every <input type='file'> in the source tree accepts image/* so iOS/Android native pickers expose camera+library+files in one sheet"
    - "No <input type='file'> carries a capture attribute — Phase 6 locked 'no capture hint' policy, Phase 7 preserves it"
    - "The shared ImageUpload component default prop remains accept='image/*' (byte-identical to Phase 6 plan 06-01)"
    - "The shared ImageUpload is the only accept-hint surface for Catalog/Colorway/DesignCopy/Video/BrandFaceView uploads; the two panels with their own hidden file input (OnModel, VideoPanel bridge) both already carry accept='image/*'"
  artifacts:
    - path: "src/components/ImageUpload.tsx"
      provides: "Default accept='image/*' on the shared upload component"
      contains: "accept = 'image/*'"
    - path: "src/components/OnModelPanel.tsx"
      provides: "Dedicated hidden file input (independent of ImageUpload) with accept='image/*'"
      contains: "accept=\"image/*\""
  key_links:
    - from: "Every <ImageUpload> call-site"
      to: "OS native file picker"
      via: "ImageUpload default accept prop = 'image/*'"
      pattern: "accept = 'image/\\*'"
    - from: "OnModelPanel multi-image grid"
      to: "OS native file picker"
      via: "dedicated <input type='file' accept='image/*' multiple>"
      pattern: "accept=\"image/\\*\""
---

<objective>
Verify TOUCH-03 (file-input accept coverage) is already satisfied across the codebase by the Phase 6 plan 06-01 work, and codify a regression lock. Specifically: every file input on the app surface opens the native OS picker via `accept="image/*"` and **no** file input carries a `capture` attribute.

Purpose: Phase 6 plan 06-01 widened both ImageUpload's default prop AND OnModelPanel's dedicated hidden input to `accept="image/*"`. Phase 7 CONTEXT locked the "no capture hint" decision. This plan is an **audit-only verify pass** that lands grep-based acceptance criteria and does NOT modify any file unless a straggler is discovered.

Output: If audit passes cleanly, zero code changes — only the verification greps + a commit recording the audit. If a straggler is found, fix it inline by adding `accept="image/*"` (and removing any stray `capture=` attribute) to bring it into compliance.

**Scope boundary — do NOT add any `capture` attribute anywhere.** The Phase 6 user decision stands: `accept="image/*"` alone triggers the native OS sheet that lets users pick camera or library. Adding `capture="user"` or `capture="environment"` would narrow that sheet on iOS and remove library access.

**Scope boundary — do NOT modify `src/components/BrandFaceView.tsx` or `src/components/AuthPage.tsx`** (other Wave 2 plans own those). If an audit reveals a file input issue in either of those files, log it and let the owning plan fix it.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auxiliary-views-touch-ergonomics/07-CONTEXT.md
@.planning/phases/06-mode-panels-on-mobile/06-01-SUMMARY.md

<interfaces>
<!-- Phase 6 plan 06-01 committed: -->
<!-- 1. src/components/ImageUpload.tsx default prop `accept = 'image/*'` (line ~19) -->
<!-- 2. src/components/OnModelPanel.tsx hidden `<input type="file" accept="image/*" multiple>` (line ~90-97) -->

<!-- Expected file-input locations based on grep: -->
<!--   src/components/ImageUpload.tsx:114 — hidden file input, accept={accept} (prop, default 'image/*') -->
<!--   src/components/OnModelPanel.tsx:92 — hidden file input, accept="image/*" -->
<!--   src/components/VideoPanel.tsx:114 — NOT an input definition, a document.querySelector lookup targeting ImageUpload's internal input via [data-video-source-upload] — no accept to set here -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit every file-input touchpoint and confirm accept='image/*' coverage + zero capture attributes</name>
  <files></files>

  <read_first>
    - src/components/ImageUpload.tsx (lines 15-20 default prop + line 114 hidden input)
    - src/components/OnModelPanel.tsx (lines 90-97 hidden input)
    - src/components/VideoPanel.tsx (lines 110-120 data-video-source-upload bridge — verify this is a querySelector lookup, NOT a new input definition)
  </read_first>

  <action>
Run the audit as a sequence of grep commands. If any acceptance criterion below fails (straggler found), fix it inline in this task before committing; otherwise commit the audit as a no-op marker.

**Step 1 — Confirm ImageUpload default prop is `accept = 'image/*'`:**

```bash
grep -n "accept = 'image/\*'" src/components/ImageUpload.tsx
```
Expect: exactly 1 match on line ~19.

**Step 2 — Confirm OnModelPanel's dedicated hidden input carries `accept="image/*"`:**

```bash
grep -n 'accept="image/\*"' src/components/OnModelPanel.tsx
```
Expect: exactly 1 match on line ~93.

**Step 3 — Confirm every `<input type="file">` across `src/` has `accept="image/*"` OR is inside ImageUpload (which injects it via prop):**

```bash
grep -nE '<input[^>]*type="file"' src/
```
Expect: exactly 2 matches — ImageUpload.tsx line 116 AND OnModelPanel.tsx line 92. Both must carry `accept="image/*"` either directly (OnModelPanel) or via the `accept={accept}` prop whose default is `'image/*'` (ImageUpload).

If a third `<input type="file">` exists anywhere else in `src/` that is NOT owned by another Wave 2 plan (07-04 owns BrandFaceView; 07-05 owns AuthPage), add `accept="image/*"` to it and update the acceptance criteria accordingly. This is a defensive branch; not expected to trigger.

**Step 4 — Confirm ZERO `capture` attributes on any file input anywhere in `src/`:**

```bash
grep -rn 'capture="' src/
grep -rn "capture='" src/
grep -rnE 'capture\s*=\s*"?(user|environment)' src/
```
Expect: 0 matches across all three greps. If any match is found, REMOVE the `capture="..."` attribute. The Phase 6 user decision was explicit — no capture hints.

**Step 5 — Confirm the VideoPanel source-picker bridge does not define a new file input (it queries ImageUpload's hidden input via data-attribute):**

```bash
grep -n 'data-video-source-upload' src/components/VideoPanel.tsx
```
Expect: exactly 2 matches (the wrapper `<div data-video-source-upload>` around ImageUpload + the `document.querySelector<HTMLInputElement>('[data-video-source-upload] input[type="file"]')` lookup).

**Step 6 — Enumerate every ImageUpload call-site to prove accept propagation:**

```bash
grep -rn '<ImageUpload' src/
```
Expect: exactly 6 matches (BrandFaceView, CatalogPanel×2, ColorwayPanel, DesignCopyPanel, VideoPanel). None of these call-sites should pass an `accept` prop override — all rely on the default `'image/*'`.

```bash
grep -rnE '<ImageUpload[^/]*accept=' src/
```
Expect: 0 matches (no call-site overrides the default).

**No file modification is performed if all 6 greps return the expected counts.**

If straggler detected:
- Missing accept → insert `accept="image/*"` on the offending `<input type="file">`.
- Extra capture → remove the attribute.
- Commit fix with message `fix(07-03): {specific straggler description}`.

If clean (expected path):
- No file modifications.
- Commit: `test(07-03): codify TOUCH-03 audit — file-input accept coverage regression-locked`

The commit should include a minimal touch: create `.planning/phases/07-auxiliary-views-touch-ergonomics/07-03-AUDIT.md` with a one-paragraph audit result + the exact grep output listing, then commit it.
  </action>

  <verify>
    <automated>
# ImageUpload default prop
grep -c "accept = 'image/\*'" src/components/ImageUpload.tsx  # expect 1
# OnModel dedicated hidden input
grep -c 'accept="image/\*"' src/components/OnModelPanel.tsx  # expect 1
# Every <input type="file"> in src has accept="image/*" OR is the ImageUpload prop-driven one
grep -rnE '<input[^>]*type="file"' src/ | wc -l  # expect 2
grep -rn '<input[^>]*type="file"[^>]*accept="image/\*"' src/components/OnModelPanel.tsx  # expect 1 match
grep -rn 'accept={accept}' src/components/ImageUpload.tsx  # expect 1 (prop propagation)
# ZERO capture attributes anywhere in src
grep -rc 'capture="' src/ | grep -v ':0$' | wc -l  # expect 0 files with capture
grep -rc "capture='" src/ | grep -v ':0$' | wc -l  # expect 0
# ImageUpload call-sites — 6 in total, none override accept
grep -rn '<ImageUpload' src/ | wc -l  # expect 6
grep -rnE '<ImageUpload[^/]*accept=' src/ | wc -l  # expect 0
# Audit document exists
test -f .planning/phases/07-auxiliary-views-touch-ergonomics/07-03-AUDIT.md
# Build still passes
npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - grep `accept = 'image/\*'` in `src/components/ImageUpload.tsx` reports exactly 1 match (default prop)
    - grep `accept="image/\*"` in `src/components/OnModelPanel.tsx` reports exactly 1 match (dedicated hidden input)
    - grep `<input[^>]*type="file"` across `src/` reports exactly 2 total matches (ImageUpload + OnModelPanel)
    - grep `capture="` across `src/` reports 0 matches (Phase 6 no-capture lock preserved)
    - grep `capture='` across `src/` reports 0 matches
    - grep `<ImageUpload` across `src/` reports exactly 6 matches (all 6 call-sites — BrandFaceView, CatalogPanel×2, ColorwayPanel, DesignCopyPanel, VideoPanel)
    - grep `<ImageUpload[^/]*accept=` across `src/` reports 0 matches (no call-site overrides the default)
    - grep `data-video-source-upload` in `src/components/VideoPanel.tsx` reports exactly 2 matches (wrapper + querySelector)
    - `.planning/phases/07-auxiliary-views-touch-ergonomics/07-03-AUDIT.md` exists with the captured grep output
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>TOUCH-03 regression-locked. Every file-input surface opens the native OS picker via `accept="image/*"` (directly or via ImageUpload prop default). Zero `capture` attributes present. The Phase 6 user decision ("no capture hint — let the OS offer both camera and library") is preserved and grep-verifiable.</done>
</task>

</tasks>

<verification>
```bash
# Final belt-and-suspenders grep sweep
grep -rnE 'capture\s*=' src/  # expect 0 matches
grep -rn '<input[^>]*type="file"' src/  # expect exactly 2 lines (ImageUpload + OnModelPanel)
grep -rn '<ImageUpload' src/ | wc -l  # expect 6
npm run build
```
</verification>

<success_criteria>
- TOUCH-03 satisfied and regression-locked via grep-based acceptance criteria.
- Zero capture attributes in `src/`.
- Every file-input path opens the OS native picker via `accept="image/*"`.
- Audit document `.planning/phases/07-auxiliary-views-touch-ergonomics/07-03-AUDIT.md` records the grep output as a dated artifact.
- Zero code-file modifications in the happy path (audit-only plan).
</success_criteria>

<output>
After completion, create `.planning/phases/07-auxiliary-views-touch-ergonomics/07-03-SUMMARY.md`
</output>
