---
phase: 07-auxiliary-views-touch-ergonomics
plan: 05
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/AuthPage.tsx
autonomous: true
requirements: [AUX-02]

must_haves:
  truths:
    - "On a 667px-tall phone (iPhone SE-class), the full AuthPage sign-in form fits within the viewport — header card visible, both inputs visible, Submit button reachable without scrolling"
    - "Both email and password <Input>s render at 16px (text-base) on every viewport so iOS Safari does not auto-zoom on focus (overrides the shared Input component's md:text-sm reduction)"
    - "Submit button meets 40px tap-floor (min-h-10)"
    - "Card width stays bounded (max-w-sm kept per current code — works on 360px-wide phones with the existing p-4 container margin; CONTEXT permitted either max-w-md with mx-4 or current max-w-sm equivalent, planner chose minimal change)"
    - "LanguageSwitcher in top-right still reachable (Plan 07-01 applied min-h-10 to it; regression-locked here)"
    - "Desktop rendering is byte-identical — the card is centered via flex + min-h-screen; the text-base override only takes precedence where it was previously reducing to text-sm at md+"
  artifacts:
    - path: "src/components/AuthPage.tsx"
      provides: "Auth page with 16px inputs + 40px submit + vertically compact form fitting on 667px-tall phones"
      contains: "text-base"
      min_lines: 80
  key_links:
    - from: "AuthPage email Input"
      to: "16px iOS no-auto-zoom"
      via: "className='text-base' override on shadcn Input"
      pattern: "className=\"text-base\""
    - from: "AuthPage password Input"
      to: "16px iOS no-auto-zoom"
      via: "className='text-base' override on shadcn Input"
      pattern: "className=\"text-base\""
    - from: "AuthPage submit Button"
      to: "40px tap floor"
      via: "className='w-full min-h-10'"
      pattern: "w-full min-h-10"
---

<objective>
Reflow `src/components/AuthPage.tsx` for AUX-02: the sign-in form fits on a 667px-tall phone (iPhone SE-class), every `<Input>` renders at 16px (`text-base`) so iOS Safari does not auto-zoom on focus, and the submit button meets a 40×40px tap floor. Preserve the existing `Card` layout (centered, `max-w-sm`) and the existing single-form structure (no sign-up toggle currently exists — CONTEXT mentioned it as optional ideal and explicitly deferred OAuth).

Purpose: Close AUX-02 ("Auth page renders within the viewport with inputs ≥ 16px font-size and the submit button reachable without scroll on a 667px-tall screen").

Output: 1 file modified. Two `<Input>` elements get explicit `className="text-base"` to override the shared Input component's `md:text-sm` reduction; the submit `<Button>` gets `min-h-10`; the outer wrapper's vertical spacing is tightened (if needed) to land within 667px.

**Scope boundary — do NOT change business logic.** `handleSubmit`, `signIn()`, state (`email`, `password`, `error`, `loading`), `useTranslation`, and `useNavigate` stay byte-identical.

**Scope boundary — do NOT add sign-up functionality.** CONTEXT hinted at a switch link ("Don't have an account? Sign up") but AuthPage currently implements sign-in only. Adding a new mode is out of scope and out of CONTEXT. The switch link wrapping instruction from CONTEXT is N/A because no switch link exists. AUX-02's success criterion is the current form fitting within the viewport — satisfy that without growing scope.

**Scope boundary — do NOT change the shared `src/components/ui/input.tsx`.** Override at the call-site only (via `className="text-base"` on each `<Input>`).

**Scope boundary — do NOT change the `LanguageSwitcher` positioning.** It sits absolutely in top-right via `.absolute top-4 right-4`; its `min-h-10` is handled by Plan 07-01.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auxiliary-views-touch-ergonomics/07-CONTEXT.md
@.planning/phases/06-mode-panels-on-mobile/06-01-SUMMARY.md

<interfaces>
<!-- Shadcn Input (src/components/ui/input.tsx) default classes include: -->
<!--   "h-8 ... text-base transition-colors outline-none ... md:text-sm ..." -->
<!-- This means on mobile the Input is 16px but REDUCES to 14px at md+ (768px+) breakpoint. -->
<!-- For AUX-02, we want 16px on EVERY viewport — not just phones — because the AUTH page is also viewed on laptops/tablets and consistency matters. -->
<!-- Override via className="text-base" on the call-site is the minimal change that wins. -->

<!-- AuthPage CURRENT structure (src/components/AuthPage.tsx): -->
<!-- line 42: outer wrapper .min-h-screen flex items-center justify-center bg-background p-4 -->
<!-- line 43-45: absolute-positioned LanguageSwitcher top-right -->
<!-- line 46: Card className="w-full max-w-sm" -->
<!-- line 47-54: CardHeader with CardTitle (text-xl) + CardDescription -->
<!-- line 56: form with flex flex-col gap-4 -->
<!-- line 59: <Input id="email" ...> — no className -->
<!-- line 70: <Input id="password" ...> — no className -->
<!-- line 83: <Button type="submit" disabled={loading} className="w-full"> -->
<!-- No sign-up switch link. CardDescription already describes "sign in to Texora Studio" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthPage input font override + submit tap-floor</name>
  <files>src/components/AuthPage.tsx</files>

  <read_first>
    - src/components/AuthPage.tsx (whole file, 92 lines)
    - src/components/ui/input.tsx (confirm shared defaults — DO NOT EDIT)
    - src/components/ui/button.tsx (confirm shared defaults — DO NOT EDIT)
  </read_first>

  <action>
Three changes to `src/components/AuthPage.tsx`. Preserve all other lines.

**Change 1 (line ~59-65): Email Input — add `className="text-base"` override.**
Current:
```tsx
              <Input
                id="email"
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
              />
```
Change to:
```tsx
              <Input
                id="email"
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
                className="text-base"
              />
```

**Change 2 (line ~70-76): Password Input — add `className="text-base"` override.**
Current:
```tsx
              <Input
                id="password"
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
              />
```
Change to:
```tsx
              <Input
                id="password"
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
                className="text-base"
              />
```

**Change 3 (line ~83-85): Submit Button — add `min-h-10` to the className.**
Current:
```tsx
            <Button type="submit" disabled={loading} className="w-full">
              {loading ? t('auth.pleaseWait') : t('common.signIn')}
            </Button>
```
Change to:
```tsx
            <Button type="submit" disabled={loading} className="w-full min-h-10">
              {loading ? t('auth.pleaseWait') : t('common.signIn')}
            </Button>
```

Do NOT touch:
- The outer wrapper `<div className="min-h-screen flex items-center justify-center bg-background p-4">` — flex-center vertical alignment is already correct; on 667px-tall phones the Card occupies ~340-380px (header ~80px + 2 form rows at ~56px gap-4 each + submit ~40px + CardContent padding ~24px ≈ 360px) which fits comfortably within 667px minus the 16px safe-area / absolute LanguageSwitcher top margin.
- The `<Card className="w-full max-w-sm">` — CONTEXT permitted either max-w-md + mx-4 or the current max-w-sm. Current `max-w-sm` (384px) works on 360px-wide phones because the outer wrapper `p-4` gives 16px side margin, and max-w-sm caps at 384px on wider phones. No change needed.
- The `<CardHeader>` / `<CardTitle>` / `<CardDescription>` — no font/size change required.
- The `<form className="flex flex-col gap-4">` — gap-4 (16px) is already tight enough; reducing to gap-3 would be a micro-optimization without measurable benefit.
- The `<LanguageSwitcher />` — handled by Plan 07-01.
- `handleSubmit`, state setters, `signIn()` call, `navigate` — business logic untouched.

**Height-fit self-audit (advisory, no code change):** With the three changes above, the form occupies roughly:
- CardHeader: 80px (CardTitle 28px + CardDescription 20px + gap + padding)
- Email row: Label 16px + gap 6px + Input 32px (h-8 with 16px font still at h-8, text-base is font only not height) = 54px
- gap-4: 16px
- Password row: 54px
- gap-4: 16px
- Error (conditional): 0-20px
- gap-4: 16px
- Submit: 40px (min-h-10 floor takes effect since h-8 = 32px < 40px)
- Total form: 54 + 16 + 54 + 16 + 16 + 40 = 196px
- Total Card: Header 80px + CardContent 196px + padding ~32px = 308px
- Total page (centered in 667px): 308px ≤ 667px minus ~64px top for LanguageSwitcher + p-4 wrapper = easily fits.

If future QA flags tightness, the next lever is reducing `gap-4` to `gap-3` (saves 8px total); this plan intentionally leaves that as a follow-up because AUX-02 explicitly says "reachable without scroll" — 308px fits 667px with ample margin.

Commit message: `feat(07-05): AUX-02 auth page — 16px inputs + 40px submit tap-floor`
  </action>

  <verify>
    <automated>
# Both Inputs carry the text-base override
grep -c 'className="text-base"' src/components/AuthPage.tsx  # expect 2 (email + password)
# Submit button has w-full min-h-10
grep -c 'className="w-full min-h-10"' src/components/AuthPage.tsx  # expect 1
# Legacy submit className gone
grep -c 'className="w-full">' src/components/AuthPage.tsx  # expect 0
# Card width unchanged
grep -c 'className="w-full max-w-sm"' src/components/AuthPage.tsx  # expect 1
# LanguageSwitcher position unchanged
grep -c 'absolute top-4 right-4' src/components/AuthPage.tsx  # expect 1
# Business logic preserved
grep -c "signIn(email, password)" src/components/AuthPage.tsx  # expect 1
grep -c "handleSubmit" src/components/AuthPage.tsx  # expect 2 (definition + onSubmit binding)
grep -c "useNavigate" src/components/AuthPage.tsx  # expect 2 (import + use)
npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - grep `className="text-base"` in `src/components/AuthPage.tsx` reports exactly 2 matches (email + password Inputs)
    - grep `className="w-full min-h-10"` in `src/components/AuthPage.tsx` reports exactly 1 match (submit Button)
    - grep `className="w-full">` in `src/components/AuthPage.tsx` reports 0 matches (legacy submit gone)
    - grep `className="w-full max-w-sm"` in `src/components/AuthPage.tsx` reports exactly 1 match (Card width preserved)
    - grep `absolute top-4 right-4` in `src/components/AuthPage.tsx` reports exactly 1 match (LanguageSwitcher position preserved)
    - grep `signIn\(email, password\)` in `src/components/AuthPage.tsx` reports exactly 1 match (business logic preserved)
    - grep `handleSubmit` in `src/components/AuthPage.tsx` reports exactly 2 matches
    - grep `useNavigate` in `src/components/AuthPage.tsx` reports exactly 2 matches
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>AuthPage email + password Inputs render at 16px on all viewports (override of shared Input's md:text-sm reduction). Submit button meets 40×40px tap floor. Card layout, LanguageSwitcher position, and all business logic are byte-identical to pre-plan state. Form fits within 667px viewport height by the height-fit self-audit (~308px total card height).</done>
</task>

</tasks>

<verification>
```bash
npm run build

# Plan 07-05 regression lock
grep -c 'className="text-base"' src/components/AuthPage.tsx  # expect 2
grep -c 'className="w-full min-h-10"' src/components/AuthPage.tsx  # expect 1

# Scope boundary: Input / Button components untouched
git diff src/components/ui/input.tsx src/components/ui/button.tsx  # expect empty
```
</verification>

<success_criteria>
- AUX-02 satisfied: email + password inputs at 16px (no iOS auto-zoom), submit button 40×40px tap floor, form fits 667px-tall viewport.
- Card layout (max-w-sm, p-4 wrapper margin, centered flex) preserved.
- Business logic byte-identical.
- Shared Shadcn Input / Button components untouched.
- Zero new imports, zero new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auxiliary-views-touch-ergonomics/07-05-SUMMARY.md`
</output>
