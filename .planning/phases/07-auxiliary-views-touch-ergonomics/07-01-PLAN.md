---
phase: 07-auxiliary-views-touch-ergonomics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/AppShell.tsx
  - src/components/LanguageSwitcher.tsx
  - src/components/ThemeToggle.tsx
  - src/components/Canvas.tsx
  - src/components/GenerationControls.tsx
  - src/components/ImageUpload.tsx
  - src/components/BrandFacePanel.tsx
autonomous: true
requirements: [TOUCH-01, TOUCH-02]

must_haves:
  truths:
    - "Every shadcn Button with size='sm', size='xs', size='default', size='icon-sm', size='icon-xs' now meets a 40px tap-target floor via min-h-10 (and min-w-10 for icon-only)"
    - "Every bare <button> in app-shell / sidebar / canvas toolbar / brand-face panel meets a 40px tap-target floor via min-h-10"
    - "User-input <input> and <textarea> elements across the app render at 16px (text-base), preventing iOS Safari auto-zoom"
    - "Desktop (lg+) rendering is byte-identical — min-h-10 is a floor that becomes a no-op when the button already exceeds 40px in a narrow container; no sizing variant was swapped"
  artifacts:
    - path: "src/components/Sidebar.tsx"
      provides: "mode select renders text-base (confirmed already at line 61); sign-out button gets min-h-10"
      contains: "min-h-10"
    - path: "src/components/AppShell.tsx"
      provides: "legacy sign-out button gets min-h-10"
      contains: "min-h-10"
    - path: "src/components/LanguageSwitcher.tsx"
      provides: "language-toggle button gets min-h-10 (sidebar account row)"
      contains: "min-h-10"
    - path: "src/components/ThemeToggle.tsx"
      provides: "theme-toggle icon button gets min-h-10 min-w-10 (icon-only)"
      contains: "min-h-10 min-w-10"
    - path: "src/components/Canvas.tsx"
      provides: "canvas toolbar grid/single view toggles + failed-clear + single-view action buttons get min-h-10 (+ min-w-10 on icon-only)"
      contains: "min-h-10"
    - path: "src/components/GenerationControls.tsx"
      provides: "aspect/quality/num-images button groups + generate button get min-h-10"
      contains: "min-h-10"
    - path: "src/components/ImageUpload.tsx"
      provides: "value-state remove X button gets min-h-10 min-w-10 (icon-only)"
      contains: "min-h-10 min-w-10"
    - path: "src/components/BrandFacePanel.tsx"
      provides: "(no change needed — already min-h-10 on collapse toggle + View All per 06-06). Audit-verified regression lock."
      contains: "min-h-10"
  key_links:
    - from: "src/components/Canvas.tsx"
      to: "canvas toolbar tap targets"
      via: "min-h-10 on view-toggle icon Buttons + bare action <button>s"
      pattern: "min-h-10"
    - from: "src/components/GenerationControls.tsx"
      to: "aspect/quality/num-images + Generate buttons"
      via: "min-h-10 append on every <Button size='xs'|'sm'> + final full-width Generate"
      pattern: "min-h-10"
    - from: "src/components/Sidebar.tsx"
      to: "account row (LanguageSwitcher, ThemeToggle, SignOut)"
      via: "min-h-10 on each child Button"
      pattern: "min-h-10"
---

<objective>
Global tap-target and font-size audit. Lift every shadcn Button call-site (`size="sm"`, `size="xs"`, `size="default"`, `size="icon-sm"`, `size="icon-xs"`) and every bare `<button>` element to a 40×40px minimum via `min-h-10` (and `min-w-10` for icon-only), confirm every user-typed `<input>`/`<textarea>` renders at `text-base` (16px) to prevent iOS auto-zoom, and do not regress the Windows 95 flat aesthetic or desktop rendering.

Purpose: Phase 7 TOUCH-01 and TOUCH-02 compliance across every interactive control not already handled in Phase 6 (which fixed Colorway input, Design Copy textarea, Video prompt textarea, OnModel/Catalog Front/Back + angle buttons, Video mode buttons, BrandFacePanel collapse/View All).

Output: 8 files modified with `min-h-10` floors (plus `min-w-10` on icon-only). Zero new dependencies. Desktop rendering byte-identical because `min-h-10` is a floor, not a fixed height.

**Scope boundary — do NOT re-touch these files (Phase 6 already handled them):**
- `src/components/ColorwayPanel.tsx` (color input + Add button, 06-03)
- `src/components/DesignCopyPanel.tsx` (modifications textarea, 06-04)
- `src/components/VideoPanel.tsx` (prompt textarea + source picker + duration/aspect/audio, 06-05)
- `src/components/OnModelPanel.tsx` (Front/Back buttons + 80px slots, 06-01)
- `src/components/CatalogPanel.tsx` (angle buttons, 06-02)

**Scope boundary — do NOT modify these files (other Wave 2 plans own them):**
- `src/components/PromptPanel.tsx` (07-02 owns)
- `src/components/BrandFaceView.tsx` (07-04 owns — full reflow + tap sweep)
- `src/components/AuthPage.tsx` (07-05 owns — reflow + tap sweep + text-base on Input)

**Scope boundary — do NOT refactor Shadcn Button or Input components themselves.** Only fix call-sites.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auxiliary-views-touch-ergonomics/07-CONTEXT.md
@.planning/phases/06-mode-panels-on-mobile/06-01-SUMMARY.md

<interfaces>
<!-- Key facts from the audit. -->
<!-- Shadcn Button `default` size is `h-8` (32px), NOT 40px as any historical note may have implied. -->
<!-- No shadcn Button variant natively hits 40px. `min-h-10` retrofit is the Phase 6 precedent. -->

Shadcn Button size variants (from src/components/ui/button.tsx):
- default: `h-8` (32px)
- xs: `h-6` (24px)
- sm: `h-7` (28px)
- lg: `h-9` (36px)
- icon: `size-8` (32x32)
- icon-sm: `size-7` (28x28)
- icon-xs: `size-6` (24x24)
- icon-lg: `size-9` (36x36)

Shadcn Input (src/components/ui/input.tsx) default classes: `h-8 ... text-base ... md:text-sm`.
NOTE: Input renders `text-base` on mobile, then REDUCES to `text-sm` at `md+`. For user-typed <input>s this is already iOS-safe on mobile. No change to the shared Input component.

Bare <button> elements are already enumerated; this plan adds min-h-10 without any new utility introduction.

Phase 6 min-h-10 precedent files (ALREADY DONE — do not re-edit):
- src/components/OnModelPanel.tsx (Front/Back buttons)
- src/components/CatalogPanel.tsx (angle buttons)
- src/components/ColorwayPanel.tsx (input + Add button)
- src/components/VideoPanel.tsx (source picker + duration + aspect + audio label)
- src/components/BrandFacePanel.tsx (collapse toggle + View All)
- src/components/WorkspaceLayout.tsx (Gallery trigger)
- src/components/Canvas.tsx (gallery close button at line 125)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep min-h-10 onto shadcn Button call-sites in global chrome (Sidebar, AppShell, LanguageSwitcher, ThemeToggle)</name>
  <files>src/components/Sidebar.tsx, src/components/AppShell.tsx, src/components/LanguageSwitcher.tsx, src/components/ThemeToggle.tsx</files>

  <read_first>
    - src/components/Sidebar.tsx (lines 60-90 mode select + VIEW_TABS Button, lines 150-172 account row)
    - src/components/AppShell.tsx (line 22 sign-out Button)
    - src/components/LanguageSwitcher.tsx (whole file, 16 lines)
    - src/components/ThemeToggle.tsx (whole file, 22 lines)
    - src/components/ui/button.tsx (to confirm h-7/h-8 size classes — DO NOT EDIT)
  </read_first>

  <action>
Append `min-h-10` to the `className` of each Button call-site listed below. For icon-only buttons (ThemeToggle, Sidebar sign-out — which shows only a LogOut icon), append `min-h-10 min-w-10` instead. Preserve all other classes byte-for-byte.

1. **src/components/Sidebar.tsx** — line ~73-85, `<Button key={tab.key} variant="ghost" size="default" ... className="justify-start rounded-none text-left text-base gap-2 ${activeView === tab.key ? 'bg-accent text-accent-foreground' : ''}">`:
   - Current: `className="justify-start rounded-none text-left text-base gap-2 ${...}"`
   - Change to: `className="justify-start rounded-none text-left text-base gap-2 min-h-10 ${...}"`

2. **src/components/Sidebar.tsx** — line ~161-169, account-row sign-out `<Button variant="ghost" size="sm" ...>` (ICON-ONLY — only `<LogOut className="size-4" />` as child):
   - Current: `className="rounded-none"`
   - Change to: `className="rounded-none min-h-10 min-w-10"`

3. **src/components/AppShell.tsx** — line ~22, sign-out Button (has text child, NOT icon-only):
   - Current: `<Button variant="outline" size="sm" onClick={handleSignOut}>`
   - Change to: `<Button variant="outline" size="sm" onClick={handleSignOut} className="min-h-10">`

4. **src/components/LanguageSwitcher.tsx** — line ~7-15, Button has TEXT child (`EN`/`TR`), NOT icon-only:
   - Current: `<Button variant="ghost" size="sm" onClick={() => setLocale(locale === 'tr' ? 'en' : 'tr')}>`
   - Change to: `<Button variant="ghost" size="sm" onClick={() => setLocale(locale === 'tr' ? 'en' : 'tr')} className="min-h-10">`

5. **src/components/ThemeToggle.tsx** — line ~12-21, Button is ICON-ONLY (`<Sun>` / `<Moon>`):
   - Current: `className="rounded-none"`
   - Change to: `className="rounded-none min-h-10 min-w-10"`

Do NOT change the shared `src/components/ui/button.tsx`. Do NOT change the mode-select `<select>` in Sidebar.tsx (already renders `text-base` per line 61 — confirm with grep, no edit needed).

Commit message: `feat(07-01): apply 40px tap floor to global chrome buttons (Sidebar/AppShell/LanguageSwitcher/ThemeToggle)`
  </action>

  <verify>
    <automated>
# Confirm min-h-10 added to exactly 4 new call-sites (Sidebar VIEW_TABS, Sidebar sign-out, AppShell sign-out, LanguageSwitcher); icon-only variants carry min-w-10 too
grep -c "min-h-10" src/components/Sidebar.tsx  # expect >= 2 (VIEW_TABS + sign-out)
grep -c "min-h-10 min-w-10" src/components/Sidebar.tsx  # expect 1 (sign-out icon-only)
grep -c "min-h-10" src/components/AppShell.tsx  # expect 1
grep -c "min-h-10" src/components/LanguageSwitcher.tsx  # expect 1
grep -c "min-h-10 min-w-10" src/components/ThemeToggle.tsx  # expect 1
# Regression: mode select still text-base (sidebar line ~61)
grep -n "text-base focus:outline-none focus:ring-1" src/components/Sidebar.tsx  # expect match
# No Button component mutation
git diff src/components/ui/button.tsx  # expect empty diff
# Build passes
npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - grep `min-h-10` in `src/components/Sidebar.tsx` reports >= 2 matches
    - grep `min-h-10 min-w-10` in `src/components/Sidebar.tsx` reports exactly 1 match (the icon-only sign-out button)
    - grep `min-h-10` in `src/components/AppShell.tsx` reports exactly 1 match
    - grep `min-h-10` in `src/components/LanguageSwitcher.tsx` reports exactly 1 match
    - grep `min-h-10 min-w-10` in `src/components/ThemeToggle.tsx` reports exactly 1 match
    - `git diff src/components/ui/button.tsx` is empty
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Global chrome buttons (Sidebar VIEW_TABS, Sidebar sign-out icon, AppShell sign-out, LanguageSwitcher, ThemeToggle) all meet 40×40px tap floor. Shared Shadcn Button component unchanged. Desktop rendering unaffected (min-h-10 is a floor).</done>
</task>

<task type="auto">
  <name>Task 2: Sweep min-h-10 onto Canvas toolbar + GenerationControls Button/bare-button call-sites</name>
  <files>src/components/Canvas.tsx, src/components/GenerationControls.tsx, src/components/ImageUpload.tsx, src/components/BrandFacePanel.tsx</files>

  <read_first>
    - src/components/Canvas.tsx (lines 44-128 toolbar region; lines 198-235 grid-item hover buttons — do NOT touch these per Phase 5 touch overlay pattern)
    - src/components/GenerationControls.tsx (lines 270-340 aspect/quality/numImages buttons + final Generate button)
    - src/components/ImageUpload.tsx (lines 83-90 value-state remove button, icon-only `<X>`)
    - src/components/BrandFacePanel.tsx (whole file — regression confirmation only, Phase 6 already added min-h-10)
  </read_first>

  <action>
1. **src/components/Canvas.tsx** — canvas toolbar (top region, ABOVE the grid content):

   a. Line ~48-56, `<Button variant={canvasViewMode === 'grid' ? 'secondary' : 'ghost'} size="icon-sm" ...>` (ICON-ONLY, `<Grid3x3>`):
      - Current: `className="rounded-none"`
      - Change to: `className="rounded-none min-h-10 min-w-10"`

   b. Line ~57-65, `<Button variant={canvasViewMode === 'single' ? 'secondary' : 'ghost'} size="icon-sm" ...>` (ICON-ONLY, `<Maximize2>`):
      - Current: `className="rounded-none"`
      - Change to: `className="rounded-none min-h-10 min-w-10"`

   c. Line ~71-78, bare `<button onClick={clearFailedGenerations}>` with TEXT (`t('workspace.canvas.delete')`):
      - Current: `className="ml-2 inline-flex h-7 items-center px-2 text-xs text-destructive border border-border bg-background hover:bg-accent rounded-none"`
      - Change to: `className="ml-2 inline-flex min-h-10 items-center px-2 text-xs text-destructive border border-border bg-background hover:bg-accent rounded-none"` (replace `h-7` with `min-h-10`)

   d. Line ~41, the `toolbarBtn` const string shared by download/delete/create-video actions in single-view:
      - Current: `"ml-1 inline-flex h-7 items-center px-2.5 text-xs border border-border bg-background hover:bg-accent rounded-none"`
      - Change to: `"ml-1 inline-flex min-h-10 items-center px-2.5 text-xs border border-border bg-background hover:bg-accent rounded-none"` (replace `h-7` with `min-h-10`)

   e. Line ~111-128, gallery close Button (already `min-h-10 min-w-10` from Phase 5.1 — REGRESSION-CONFIRM only, no edit):
      - Grep verification only: expect `min-h-10 min-w-10 rounded-none lg:hidden` on this Button.

   f. **DO NOT** touch the grid-item hover-reveal buttons on lines ~198-235 — those are text-[11px] inline links inside the touch overlay (Phase 5-01 established the opacity-100 lg:opacity-0 pattern for reachability, and enlarging them would break the overlay's visual density. TOUCH-01's "decorative icons may be smaller only when paired with a larger hit area" applies here — the entire overlay bar is the tap target).

2. **src/components/GenerationControls.tsx**:

   a. Line ~274-284, ASPECT_RATIOS `<Button key={ratio} variant={...} size="xs" ... className="flex-1 rounded-none">`:
      - Current: `className="flex-1 rounded-none"`
      - Change to: `className="flex-1 rounded-none min-h-10"`

   b. Line ~292-302, QUALITY_LEVELS `<Button key={level} variant={...} size="sm" ... className="flex-1 rounded-none">`:
      - Current: `className="flex-1 rounded-none"`
      - Change to: `className="flex-1 rounded-none min-h-10"`

   c. Line ~311-321, IMAGE_COUNTS `<Button key={count} variant={...} size="xs" ... className="flex-1 rounded-none">`:
      - Current: `className="flex-1 rounded-none"`
      - Change to: `className="flex-1 rounded-none min-h-10"`

   d. Line ~332-338, final Generate Button `<Button onClick={handleGenerate} disabled={isGenerating} className="w-full rounded-none">`:
      - Current: `className="w-full rounded-none"`
      - Change to: `className="w-full rounded-none min-h-10"`

3. **src/components/ImageUpload.tsx** — line ~83-90, value-state remove `<Button variant="ghost" size="icon-xs" ...>` (ICON-ONLY, `<X>`):
   - Current: `className="absolute right-1 top-1 rounded-none border border-border bg-background"`
   - Change to: `className="absolute right-1 top-1 rounded-none border border-border bg-background min-h-10 min-w-10"`

4. **src/components/BrandFacePanel.tsx** — REGRESSION CONFIRMATION ONLY, no edit.
   - Grep verification: `min-h-10` appears on the collapse toggle (line ~36) AND the "View All" button (line ~89). Both were added in Phase 6 plan 06-06. If either is missing, this plan must re-add it with the same pattern.

Commit message: `feat(07-01): apply 40px tap floor to Canvas toolbar + GenerationControls + ImageUpload remove`
  </action>

  <verify>
    <automated>
# Canvas toolbar: 2 icon-only view toggles (min-h-10 min-w-10), clear-failed bare button (min-h-10), toolbarBtn const (min-h-10), gallery-close already present from Phase 5.1
grep -c "min-h-10 min-w-10 rounded-none" src/components/Canvas.tsx  # expect >= 2 (grid + single toggles) — note Phase 5.1 close button matches "min-h-10 min-w-10 rounded-none lg:hidden"
# The toolbarBtn const must carry min-h-10 not h-7
grep -c "inline-flex h-7" src/components/Canvas.tsx  # expect 0 (h-7 fully replaced)
grep -c "inline-flex min-h-10 items-center px-2.5 text-xs" src/components/Canvas.tsx  # expect 1 (toolbarBtn)
grep -c "inline-flex min-h-10 items-center px-2 text-xs text-destructive" src/components/Canvas.tsx  # expect 1 (clear-failed bare button)
# GenerationControls: 4 call-sites get min-h-10 (aspect, quality, numImages, generate)
grep -c "rounded-none min-h-10" src/components/GenerationControls.tsx  # expect 3 (aspect/quality/numImages flex-1)
grep -c "w-full rounded-none min-h-10" src/components/GenerationControls.tsx  # expect 1 (generate)
# ImageUpload value-state remove X has min-h-10 min-w-10
grep -c "rounded-none border border-border bg-background min-h-10 min-w-10" src/components/ImageUpload.tsx  # expect 1
# BrandFacePanel regression: min-h-10 still on collapse + View All
grep -c "min-h-10" src/components/BrandFacePanel.tsx  # expect >= 2
# Grid item hover overlays UNTOUCHED
grep -c "text-\[11px\] text-white/80" src/components/Canvas.tsx  # expect 3 (delete/createVideo/download stay exactly as is)
npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - grep `inline-flex h-7` in `src/components/Canvas.tsx` reports 0 matches (h-7 fully replaced by min-h-10 on the toolbarBtn const and clear-failed bare button)
    - grep `min-h-10 min-w-10 rounded-none` in `src/components/Canvas.tsx` reports >= 2 matches (the two view-mode icon toggles)
    - grep `rounded-none min-h-10` in `src/components/GenerationControls.tsx` reports exactly 3 matches (aspect/quality/numImages flex-1 buttons)
    - grep `w-full rounded-none min-h-10` in `src/components/GenerationControls.tsx` reports exactly 1 match (final Generate button)
    - grep `rounded-none border border-border bg-background min-h-10 min-w-10` in `src/components/ImageUpload.tsx` reports exactly 1 match
    - grep `min-h-10` in `src/components/BrandFacePanel.tsx` reports >= 2 matches (regression lock on Phase 6 work)
    - grep `text-\[11px\] text-white/80` in `src/components/Canvas.tsx` reports exactly 3 matches (grid-item hover overlay text buttons untouched)
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Canvas toolbar icon-only view toggles, toolbarBtn single-view actions, clear-failed button, ImageUpload remove-X, and all GenerationControls Buttons (aspect/quality/numImages/Generate) meet 40×40px tap floor. Grid-item hover overlay untouched (Phase 5 touch-overlay pattern preserved). BrandFacePanel regression-confirmed.</done>
</task>

<task type="auto">
  <name>Task 3: Font-size audit — confirm user-typed inputs/textareas render at text-base (16px); no edit expected</name>
  <files>src/components/Sidebar.tsx</files>

  <read_first>
    - src/components/ui/input.tsx (confirm shared Input: `text-base ... md:text-sm` — mobile-first 16px, reduces at md+)
    - src/components/PromptPanel.tsx (text-sm on line 19 — BUT owned by Plan 07-02, DO NOT edit here)
    - src/components/Sidebar.tsx (line 61: mode select has `text-base` — confirm)
    - src/components/ColorwayPanel.tsx (line 84: Phase 6 already text-base min-h-10 — confirm regression lock)
    - src/components/DesignCopyPanel.tsx (Phase 6 already text-base field-sizing-content min-h-[5rem] — confirm regression lock)
    - src/components/VideoPanel.tsx (Phase 6 already text-base field-sizing-content min-h-[5rem] — confirm regression lock)
  </read_first>

  <action>
This task is an AUDIT. Per CONTEXT the font sweep is SELECTIVE — bump `text-sm` → `text-base` ONLY on user-typed `<input>` / `<textarea>` elements where the font determines typed-character size (and therefore iOS zoom behavior). NOT labels, NOT button labels, NOT read-only text.

**Run the audit:**

1. Grep for every `<input>` and `<textarea>` element in `src/` and map its text-size class:

   ```
   grep -nE "<(input|textarea)\b" src/components/
   ```

   Enumerated call-sites (based on Plan's upstream read):
   - `src/components/AuthPage.tsx` — `<Input>` components (lines 59, 70) — **OWNED BY 07-05**, do not edit here
   - `src/components/BrandFaceView.tsx` — `<input type="text">` (line 63, `text-sm`) — **OWNED BY 07-04**, do not edit here
   - `src/components/ColorwayPanel.tsx` — `<input type="text">` line 78: already `text-base min-h-10` ✓
   - `src/components/DesignCopyPanel.tsx` — `<textarea>` line 24: already `text-base ... field-sizing-content ... min-h-[5rem]` ✓
   - `src/components/ImageUpload.tsx` — `<input type="file">` line 114: hidden, not user-typed — no text-size rule applies
   - `src/components/OnModelPanel.tsx` — `<input type="file">` line 90: hidden, not user-typed — no text-size rule applies
   - `src/components/PromptPanel.tsx` — `<textarea>` line 14: `text-sm` — **OWNED BY 07-02**, do not edit here
   - `src/components/VideoPanel.tsx` — `<textarea>` line 161: already `text-base ... min-h-[5rem] field-sizing-content` ✓; `<input type="checkbox">` line 208: decorative toggle, no font rule applies
   - `src/components/Sidebar.tsx` — `<select>` line ~55: `text-base` ✓ (select renders option chrome from browser, text-base keeps label sizing correct)

2. Confirm the shared `src/components/ui/input.tsx` component renders `text-base` by default (and only reduces to `text-sm` at `md+`). This satisfies AuthPage's `<Input>` usage on mobile BUT AUX-02 requires explicit `text-base` override so the reduction at `md` does not bite on landscape phones. **That override is handled by Plan 07-05** for AuthPage.

3. **No file edit required in this task.** The selective font sweep produces 0 edits within this plan's owned file list. The 3 not-yet-handled inputs (PromptPanel textarea, BrandFaceView name input, AuthPage Input) are owned by Plans 07-02, 07-04, 07-05 respectively.

4. Record audit result as an inline comment in `src/components/Sidebar.tsx` directly ABOVE the existing `<select>` block:

   Find (line ~55, just before the `<select ...>`):
   ```tsx
           <select
             value={currentMode}
   ```
   Prepend a single-line comment on the line IMMEDIATELY above the `<select>`:
   ```tsx
           {/* TOUCH-02 audit: text-base confirmed on mode select — iOS option-label render stays 16px. */}
           <select
             value={currentMode}
   ```

   This is the ONLY edit in Task 3. It codifies the audit result inline next to the element so a future reviewer sees why the class was intentionally left at text-base.

Commit message: `docs(07-01): codify TOUCH-02 font-size audit — no edits required in 07-01 scope`
  </action>

  <verify>
    <automated>
# Sidebar audit comment lands exactly above the select block
grep -c "TOUCH-02 audit: text-base confirmed on mode select" src/components/Sidebar.tsx  # expect 1
grep -n "text-base focus:outline-none focus:ring-1" src/components/Sidebar.tsx  # expect match (the actual select classes)
# Regression confirmation — Phase 6 user-input font-sizes still text-base
grep -c "text-base min-h-10" src/components/ColorwayPanel.tsx  # expect 1 (color input)
grep -c "text-base min-h-\[5rem\] field-sizing-content" src/components/DesignCopyPanel.tsx  # expect 1
grep -c "text-base min-h-\[5rem\] field-sizing-content" src/components/VideoPanel.tsx  # expect 1 (prompt textarea)
# Shared Input component still renders text-base on mobile
grep -c "text-base transition-colors outline-none" src/components/ui/input.tsx  # expect 1
# PromptPanel still has legacy text-sm (owned by 07-02, not this plan)
grep -c "font-mono text-sm text-foreground" src/components/PromptPanel.tsx  # expect 1
# BrandFaceView still has legacy text-sm on name input (owned by 07-04)
grep -c "text-sm outline-none focus:border-foreground rounded-none" src/components/BrandFaceView.tsx  # expect 1
npm run build
    </automated>
  </verify>

  <acceptance_criteria>
    - grep `TOUCH-02 audit: text-base confirmed on mode select` in `src/components/Sidebar.tsx` reports exactly 1 match
    - grep `text-base min-h-10` in `src/components/ColorwayPanel.tsx` reports exactly 1 match (regression lock)
    - grep `text-base min-h-\[5rem\] field-sizing-content` in `src/components/DesignCopyPanel.tsx` reports exactly 1 match (regression lock)
    - grep `text-base min-h-\[5rem\] field-sizing-content` in `src/components/VideoPanel.tsx` reports exactly 1 match (regression lock — prompt textarea)
    - grep `text-base transition-colors outline-none` in `src/components/ui/input.tsx` reports exactly 1 match (shared Input still ships 16px on mobile)
    - grep `font-mono text-sm text-foreground` in `src/components/PromptPanel.tsx` reports exactly 1 match (PromptPanel still at legacy text-sm — 07-02 owns)
    - grep `text-sm outline-none focus:border-foreground rounded-none` in `src/components/BrandFaceView.tsx` reports exactly 1 match (BrandFaceView still at legacy text-sm — 07-04 owns)
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>Font-size audit documented inline in Sidebar.tsx. Confirmed Phase 6 text-base regressions on Colorway/DesignCopy/Video. Shared Input component confirmed 16px on mobile. Three outstanding user-typed inputs (PromptPanel textarea, BrandFaceView name input, AuthPage Input) are explicitly scoped to Plans 07-02, 07-04, 07-05.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

```bash
# Full audit grep — every file in this plan should have min-h-10 somewhere (except the audit-only Task 3 files)
grep -l "min-h-10" src/components/Sidebar.tsx src/components/AppShell.tsx src/components/LanguageSwitcher.tsx src/components/ThemeToggle.tsx src/components/Canvas.tsx src/components/GenerationControls.tsx src/components/ImageUpload.tsx
# Expect all 7 files listed.

# No shadcn Button / Input mutation
git diff src/components/ui/button.tsx src/components/ui/input.tsx
# Expect empty.

# Desktop regression check — no file owned by Phase 6 was modified
git diff src/components/ColorwayPanel.tsx src/components/DesignCopyPanel.tsx src/components/VideoPanel.tsx src/components/OnModelPanel.tsx src/components/CatalogPanel.tsx
# Expect empty.

# Full typecheck + build
npm run build
# Expect exit 0.
```
</verification>

<success_criteria>
- TOUCH-01 satisfied globally (except the 3 explicitly deferred files owned by 07-02/07-04/07-05): every shadcn Button call-site and every bare `<button>` in global chrome/canvas/controls meets `min-h-10` (+ `min-w-10` on icon-only) floor.
- TOUCH-02 satisfied globally: every user-typed input/textarea under this plan's scope is at `text-base` (the 3 stragglers are scoped to their own plans).
- Zero regression on Phase 6 Colorway/DesignCopy/Video/OnModel/Catalog/BrandFacePanel work.
- Zero modification of shared Shadcn Button or Input components.
- Zero modification of grid-item hover overlay buttons (Phase 5 touch-overlay pattern preserved).
- `npm run build` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/07-auxiliary-views-touch-ergonomics/07-01-SUMMARY.md`
</output>
