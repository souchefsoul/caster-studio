---
phase: 03-full-generation-suite-organization
plan: 05
type: execute
wave: 3
depends_on: ["03-04"]
files_modified:
  - src/components/DesignCopyPanel.tsx
  - src/lib/fal.ts
  - src/stores/workspaceStore.ts
  - src/components/Sidebar.tsx
  - src/components/GenerationControls.tsx
  - src/lib/translations/tr.ts
  - src/lib/translations/en.ts
autonomous: true
requirements: [GEN-04]

must_haves:
  truths:
    - "User can select Design Language Copier mode from sidebar"
    - "User can upload a reference product image whose visual style they want to copy"
    - "User can describe modifications (text changes, color tweaks, small adjustments)"
    - "Generated image preserves the reference's visual language while applying the described modifications"
  artifacts:
    - path: "src/components/DesignCopyPanel.tsx"
      provides: "Design Copy mode sidebar panel with reference image upload and modification text"
      exports: ["DesignCopyPanel"]
    - path: "src/lib/fal.ts"
      provides: "generateDesignCopy API function"
      exports: ["generateDesignCopy"]
  key_links:
    - from: "src/components/DesignCopyPanel.tsx"
      to: "src/stores/workspaceStore.ts"
      via: "reads/writes reference image and modification text"
      pattern: "designCopyReferenceImage"
    - from: "src/components/GenerationControls.tsx"
      to: "src/lib/fal.ts"
      via: "calls generateDesignCopy when mode is design-copy"
      pattern: "generateDesignCopy"
    - from: "src/components/Sidebar.tsx"
      to: "src/components/DesignCopyPanel.tsx"
      via: "conditional render when mode is design-copy"
      pattern: "DesignCopyPanel"
---

<objective>
Implement Design Language Copier mode: upload a reference product, describe modifications, generate a new image preserving the original's visual style.

Purpose: Design Language Copier enables textile companies to iterate on existing product imagery — change colors, text, or minor details while maintaining brand consistency and visual coherence with the original design.

Output: DesignCopyPanel component, generateDesignCopy API function, mode enabled in sidebar.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/fal.ts
@src/types/workspace.ts
@src/stores/workspaceStore.ts
@src/components/Sidebar.tsx
@src/components/OnModelPanel.tsx
@src/components/GenerationControls.tsx
@src/components/ImageUpload.tsx
@src/lib/translations/tr.ts
@src/lib/translations/en.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/fal.ts:
```typescript
export interface FalImageResult {
  images: Array<{ url: string; width: number; height: number }>
  seed: number
  prompt: string
}

// Auth pattern: supabase.auth.getSession() -> Bearer token -> fal-proxy
// Endpoint: fal-ai/nano-banana-pro/edit with image_url for reference images
```

From src/components/OnModelPanel.tsx:
```typescript
// Pattern: uses ImageUpload with value/onChange, reads/writes workspace store state
// Label text above upload, hint text below
```

From src/components/Sidebar.tsx:
```typescript
const GENERATION_MODES = [
  { key: 'on-model', ..., enabled: true },
  { key: 'catalog', ..., enabled: false },  // will be true after Plan 04
  { key: 'colorway', ..., enabled: false }, // will be true after Plan 04
  { key: 'design-copy', ..., enabled: false },
  { key: 'text-to-image', ..., enabled: false },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add design-copy state to store, create API function, build DesignCopyPanel</name>
  <files>src/stores/workspaceStore.ts, src/lib/fal.ts, src/components/DesignCopyPanel.tsx</files>
  <read_first>
    - src/stores/workspaceStore.ts
    - src/lib/fal.ts
    - src/types/workspace.ts
    - src/components/OnModelPanel.tsx
    - src/components/ImageUpload.tsx
  </read_first>
  <action>
1. Add design-copy state to `src/stores/workspaceStore.ts`:

   - `designCopyReferenceImage: string | null` — reference product image data URL. Default null.
   - `setDesignCopyReferenceImage: (url: string | null) => void`
   - `designCopyModifications: string` — text describing what to change. Default empty string.
   - `setDesignCopyModifications: (text: string) => void`

   Add to interface and implementation.

2. Add API function to `src/lib/fal.ts`:

   - `generateDesignCopy(params: GenerationParams, referenceImageDataUrl: string, modifications: string): Promise<FalImageResult>`:
     - Same auth pattern as generateOnModel (get session, Bearer token)
     - Endpoint: `fal-ai/nano-banana-pro/edit`
     - Prompt construction: `${params.prompt}. Maintain the exact visual style, design language, and aesthetic of the reference image. Apply these modifications: ${modifications}`
     - Body: `{ endpoint, input: { prompt, image_url: referenceImageDataUrl, num_inference_steps, image_size } }`
     - Same size/steps mapping as other functions

3. Create `src/components/DesignCopyPanel.tsx`:

   - Uses workspace store for `designCopyReferenceImage`, `setDesignCopyReferenceImage`, `designCopyModifications`, `setDesignCopyModifications`
   - Uses `useTranslation()` for all text

   **UI layout (Windows 95 flat style):**
   - Section label: "Reference Image" (translated, text-xs font-medium)
   - ImageUpload component for reference image (reuse existing component)
   - Section label: "Modifications" (translated, text-xs font-medium)
   - Textarea for modifications text:
     - 3 rows
     - rounded-none border border-input bg-background
     - font-mono text-sm
     - Placeholder: "Describe changes: color, text, details..." (translated)
   - Hint text at bottom: "Upload a product image and describe what to change. The visual style will be preserved." (translated)

   - Style: Same as OnModelPanel — flex flex-col gap-3, Windows 95 flat.
  </action>
  <verify>
    <automated>cd "C:/Users/mertb/caster.studio" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/workspaceStore.ts contains designCopyReferenceImage, setDesignCopyReferenceImage, designCopyModifications, setDesignCopyModifications
    - src/lib/fal.ts exports generateDesignCopy function
    - generateDesignCopy uses "fal-ai/nano-banana-pro/edit" endpoint
    - src/components/DesignCopyPanel.tsx exists and exports DesignCopyPanel
    - DesignCopyPanel.tsx contains ImageUpload usage and textarea for modifications
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Design copy store state, API function, and panel component exist with proper typing and UI</done>
</task>

<task type="auto">
  <name>Task 2: Enable design-copy mode in sidebar, wire generation routing, add translations</name>
  <files>src/components/Sidebar.tsx, src/components/GenerationControls.tsx, src/lib/translations/tr.ts, src/lib/translations/en.ts</files>
  <read_first>
    - src/components/Sidebar.tsx
    - src/components/GenerationControls.tsx
    - src/components/DesignCopyPanel.tsx
    - src/lib/fal.ts
    - src/stores/workspaceStore.ts
    - src/lib/translations/tr.ts
    - src/lib/translations/en.ts
  </read_first>
  <action>
1. Update `src/components/Sidebar.tsx`:

   - Enable design-copy mode in GENERATION_MODES array: set `enabled: true` for `design-copy` entry.
   - Import `DesignCopyPanel`
   - Add conditional render in the scrollable prompt area (same pattern as OnModelPanel):
   ```
   {currentMode === 'design-copy' && (
     <>
       <DesignCopyPanel />
       <div className="my-3 border-t border-border" />
     </>
   )}
   ```

2. Update `src/components/GenerationControls.tsx`:

   - Import `generateDesignCopy` from `@/lib/fal`
   - Import design copy state from workspace store: `designCopyReferenceImage`, `designCopyModifications`
   - Add design-copy case to `handleGenerate`:
     - Validate: `designCopyReferenceImage` must exist. If missing, set error with translated message.
     - Call `generateDesignCopy(params, designCopyReferenceImage, designCopyModifications)`
     - Create generation entry with mode 'design-copy', update on complete/fail (same pattern as on-model)

3. Add translation keys to `tr.ts`:

   - `workspace.designCopy.referenceImage`: "Referans Gorsel"
   - `workspace.designCopy.modifications`: "Degisiklikler"
   - `workspace.designCopy.modPlaceholder`: "Degisiklikleri tanimlayin: renk, yazi, detaylar..."
   - `workspace.designCopy.hint`: "Bir urun gorseli yukleyin ve neleri degistirmek istediginizi tanimlayin. Gorsel dil korunacaktir."
   - `workspace.designCopy.noReferenceImage`: "Lutfen bir referans gorseli yukleyin"

4. Add matching English keys to `en.ts`:

   - `workspace.designCopy.referenceImage`: "Reference Image"
   - `workspace.designCopy.modifications`: "Modifications"
   - `workspace.designCopy.modPlaceholder`: "Describe changes: color, text, details..."
   - `workspace.designCopy.hint`: "Upload a product image and describe what to change. The visual style will be preserved."
   - `workspace.designCopy.noReferenceImage`: "Please upload a reference image"
  </action>
  <verify>
    <automated>cd "C:/Users/mertb/caster.studio" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx has design-copy mode with enabled: true
    - Sidebar.tsx imports and conditionally renders DesignCopyPanel
    - GenerationControls.tsx imports generateDesignCopy
    - GenerationControls.tsx contains "design-copy" mode handling with designCopyReferenceImage
    - tr.ts contains "workspace.designCopy.referenceImage" key
    - en.ts contains "workspace.designCopy.referenceImage" key
    - npm run build succeeds with exit code 0
  </acceptance_criteria>
  <done>Design Language Copier mode fully enabled: selectable in sidebar, shows reference upload + modifications textarea, generates preserving visual style, all text translated</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `grep -r "design-copy.*enabled: true" src/components/Sidebar.tsx` shows mode enabled
3. `grep -r "generateDesignCopy" src/lib/fal.ts` shows the function
4. `grep -r "DesignCopyPanel" src/components/Sidebar.tsx` shows conditional render
5. `grep -r "designCopyReferenceImage" src/components/GenerationControls.tsx` shows routing
</verification>

<success_criteria>
- Design Language Copier mode is selectable in sidebar navigation
- Design Copy mode shows reference image upload and modifications textarea
- Generating in Design Copy mode sends reference image + modifications to FAL API
- Generated image appears on canvas
- All UI text translated in Turkish and English
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-generation-suite-organization/03-05-SUMMARY.md`
</output>
