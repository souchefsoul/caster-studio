---
phase: 03-full-generation-suite-organization
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/CatalogPanel.tsx
  - src/components/ColorwayPanel.tsx
  - src/lib/fal.ts
  - src/stores/workspaceStore.ts
  - src/components/Sidebar.tsx
  - src/lib/translations/tr.ts
  - src/lib/translations/en.ts
autonomous: true
requirements: [GEN-02, GEN-03]

must_haves:
  truths:
    - "User can select Catalog mode and generate multi-angle product shots from a single product image"
    - "User can select Colorway mode and generate the same garment in multiple color variations"
    - "Catalog mode lets user pick which angles to generate (front, back, side-left, side-right, 3/4 front, 3/4 back)"
    - "Colorway mode lets user input color names/hex values and generates one image per color"
    - "Both modes produce multiple generation entries visible in the grid"
  artifacts:
    - path: "src/components/CatalogPanel.tsx"
      provides: "Catalog mode sidebar panel with angle selection"
      exports: ["CatalogPanel"]
    - path: "src/components/ColorwayPanel.tsx"
      provides: "Colorway mode sidebar panel with color input"
      exports: ["ColorwayPanel"]
    - path: "src/lib/fal.ts"
      provides: "generateCatalog and generateColorway API functions"
      exports: ["generateCatalog", "generateColorway"]
  key_links:
    - from: "src/components/CatalogPanel.tsx"
      to: "src/stores/workspaceStore.ts"
      via: "reads/writes catalog-specific params (selectedAngles)"
      pattern: "catalogAngles"
    - from: "src/components/ColorwayPanel.tsx"
      to: "src/stores/workspaceStore.ts"
      via: "reads/writes colorway colors list"
      pattern: "colorwayColors"
    - from: "src/components/GenerationControls.tsx"
      to: "src/lib/fal.ts"
      via: "calls generateCatalog or generateColorway based on currentMode"
      pattern: "generateCatalog|generateColorway"
    - from: "src/components/Sidebar.tsx"
      to: "src/components/CatalogPanel.tsx"
      via: "conditional render when mode is catalog"
      pattern: "CatalogPanel"
---

<objective>
Implement Catalog Mode (multi-angle product shots) and Colorway Generator (color variations) as new generation modes.

Purpose: Catalog mode lets textile companies create consistent multi-angle product imagery from a single photo. Colorway mode enables rapid visualization of the same garment in different colors, critical for fashion wholesale.

Output: CatalogPanel and ColorwayPanel components, generateCatalog and generateColorway API functions, modes enabled in sidebar navigation.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/fal.ts
@src/types/workspace.ts
@src/stores/workspaceStore.ts
@src/components/Sidebar.tsx
@src/components/OnModelPanel.tsx
@src/components/GenerationControls.tsx
@src/components/ImageUpload.tsx
@src/lib/translations/tr.ts
@src/lib/translations/en.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/workspace.ts:
```typescript
export type GenerationMode = 'on-model' | 'catalog' | 'colorway' | 'design-copy' | 'text-to-image'

export interface GenerationParams {
  prompt: string
  model: string
  aspectRatio: string
  quality: 'draft' | 'standard' | 'high'
  productImageUrl: string | null
}
```

From src/lib/fal.ts:
```typescript
export interface FalImageResult {
  images: Array<{ url: string; width: number; height: number }>
  seed: number
  prompt: string
}

export async function generateImage(params: GenerationParams): Promise<FalImageResult>
export async function generateOnModel(params: GenerationParams, productImageDataUrl: string): Promise<FalImageResult>
```

From src/components/Sidebar.tsx:
```typescript
const GENERATION_MODES: { key: GenerationMode; labelKey: string; enabled: boolean }[] = [
  { key: 'on-model', labelKey: 'workspace.sidebar.onModel', enabled: true },
  { key: 'catalog', labelKey: 'workspace.sidebar.catalog', enabled: false },
  { key: 'colorway', labelKey: 'workspace.sidebar.colorway', enabled: false },
  { key: 'design-copy', labelKey: 'workspace.sidebar.designCopy', enabled: false },
  { key: 'text-to-image', labelKey: 'workspace.sidebar.textToImage', enabled: false },
]
```

From src/components/GenerationControls.tsx:
```typescript
// handleGenerate checks currentMode and routes to the right API function
// Pattern: if (currentMode === 'on-model' && productImageDataUrl) generateOnModel(...) else generateImage(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catalog and colorway state to store, create API functions, build mode panels</name>
  <files>src/stores/workspaceStore.ts, src/lib/fal.ts, src/components/CatalogPanel.tsx, src/components/ColorwayPanel.tsx, src/types/workspace.ts</files>
  <read_first>
    - src/stores/workspaceStore.ts
    - src/lib/fal.ts
    - src/types/workspace.ts
    - src/components/OnModelPanel.tsx
    - src/components/ImageUpload.tsx
  </read_first>
  <action>
1. Add mode-specific state to `src/stores/workspaceStore.ts`:

   **Catalog state:**
   - `catalogAngles: string[]` — default `['front', 'back', 'side-left', 'side-right']`. These are the angles to generate.
   - `setCatalogAngles: (angles: string[]) => void`
   - `catalogProductImage: string | null` — product image data URL for catalog mode
   - `setCatalogProductImage: (url: string | null) => void`

   **Colorway state:**
   - `colorwayColors: string[]` — default `['Red', 'Blue', 'Green']`. Color names or descriptions.
   - `setColorwayColors: (colors: string[]) => void`
   - `colorwayProductImage: string | null` — product image data URL for colorway mode
   - `setColorwayProductImage: (url: string | null) => void`

   Add all to interface and implementation.

2. Add API functions to `src/lib/fal.ts`:

   - `generateCatalog(params: GenerationParams, productImageDataUrl: string, angle: string): Promise<FalImageResult>` — Same pattern as generateOnModel but appends angle instruction to prompt. The prompt sent to FAL should be: `${params.prompt}, ${angle} view, product photography, consistent lighting` and use the `fal-ai/nano-banana-pro/edit` endpoint with the product image as `image_url`. Uses same auth pattern, same size/steps mapping.

   - `generateColorway(params: GenerationParams, productImageDataUrl: string, color: string): Promise<FalImageResult>` — Same pattern but appends color instruction: `${params.prompt}, in ${color} color, same garment design, product photography`. Uses `fal-ai/nano-banana-pro/edit` with product image.

3. Create `src/components/CatalogPanel.tsx`:

   - Uses workspace store for `catalogAngles`, `setCatalogAngles`, `catalogProductImage`, `setCatalogProductImage`
   - Available angles as constants: `['front', 'back', 'side-left', 'side-right', '3/4-front', '3/4-back']` with translated labels
   - UI layout:
     - Product image upload (reuse ImageUpload component) with label "Product Image"
     - "Angles" section: checkbox list of 6 angles. Each checkbox toggles that angle in/out of `catalogAngles`. Checkboxes are simple `<input type="checkbox">` with text-xs labels. All use rounded-none.
     - Hint text: "Select angles to generate. One image per angle." (translated)
   - Style: Windows 95 flat. No rounded corners.

4. Create `src/components/ColorwayPanel.tsx`:

   - Uses workspace store for `colorwayColors`, `setColorwayColors`, `colorwayProductImage`, `setColorwayProductImage`
   - UI layout:
     - Product image upload (reuse ImageUpload component) with label "Product Image"
     - "Colors" section:
       - List of current colors, each as a row with color name text and an X remove button
       - Text input + "Add" button at bottom to add a new color. Input placeholder: "e.g. Navy Blue, #FF0000"
       - Minimum 1 color required, maximum 8
     - Hint text: "Add colors to generate. One image per color variation." (translated)
   - Style: Windows 95 flat. No rounded corners.
  </action>
  <verify>
    <automated>cd "C:/Users/mertb/texora.studio" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/workspaceStore.ts contains catalogAngles, setCatalogAngles, catalogProductImage, setCatalogProductImage
    - src/stores/workspaceStore.ts contains colorwayColors, setColorwayColors, colorwayProductImage, setColorwayProductImage
    - src/lib/fal.ts exports generateCatalog and generateColorway functions
    - src/components/CatalogPanel.tsx exists and exports CatalogPanel with angle checkboxes
    - src/components/ColorwayPanel.tsx exists and exports ColorwayPanel with color list
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Catalog and colorway store state, API functions, and panel components exist with proper typing</done>
</task>

<task type="auto">
  <name>Task 2: Enable modes in sidebar, wire generation routing, add translations</name>
  <files>src/components/Sidebar.tsx, src/components/GenerationControls.tsx, src/lib/translations/tr.ts, src/lib/translations/en.ts</files>
  <read_first>
    - src/components/Sidebar.tsx
    - src/components/GenerationControls.tsx
    - src/components/CatalogPanel.tsx
    - src/components/ColorwayPanel.tsx
    - src/lib/fal.ts
    - src/stores/workspaceStore.ts
    - src/lib/translations/tr.ts
    - src/lib/translations/en.ts
  </read_first>
  <action>
1. Update `src/components/Sidebar.tsx`:

   - Enable catalog and colorway modes in GENERATION_MODES array: set `enabled: true` for both `catalog` and `colorway` entries.
   - Import `CatalogPanel` and `ColorwayPanel`
   - In the scrollable prompt area, add conditional rendering for the new modes (same pattern as OnModelPanel):
   ```
   {currentMode === 'catalog' && (
     <>
       <CatalogPanel />
       <div className="my-3 border-t border-border" />
     </>
   )}
   {currentMode === 'colorway' && (
     <>
       <ColorwayPanel />
       <div className="my-3 border-t border-border" />
     </>
   )}
   ```

2. Update `src/components/GenerationControls.tsx`:

   - Import `generateCatalog`, `generateColorway` from `@/lib/fal`
   - Import catalog/colorway state from workspace store: `catalogAngles`, `catalogProductImage`, `colorwayColors`, `colorwayProductImage`
   - Update `handleGenerate` to handle the new modes:

   **Catalog mode:**
   - Validate: `catalogProductImage` must exist, `catalogAngles` must have at least 1 item
   - For each angle in `catalogAngles`, create a separate generation entry (addGeneration with status pending) and call `generateCatalog(params, catalogProductImage, angle)`. Use `Promise.allSettled` to run all in parallel.
   - Update each generation on resolve/reject.

   **Colorway mode:**
   - Validate: `colorwayProductImage` must exist, `colorwayColors` must have at least 1 item
   - For each color in `colorwayColors`, create a separate generation entry and call `generateColorway(params, colorwayProductImage, color)`. Use `Promise.allSettled` for parallel execution.
   - Update each generation on resolve/reject.

   - Add validation error messages for missing product images in each mode (reuse existing pattern).

3. Add translation keys to `tr.ts`:

   - `workspace.catalog.productImage`: "Urun Gorseli"
   - `workspace.catalog.angles`: "Acilar"
   - `workspace.catalog.angleFront`: "On"
   - `workspace.catalog.angleBack`: "Arka"
   - `workspace.catalog.angleSideLeft`: "Sol Yan"
   - `workspace.catalog.angleSideRight`: "Sag Yan"
   - `workspace.catalog.angle34Front`: "3/4 On"
   - `workspace.catalog.angle34Back`: "3/4 Arka"
   - `workspace.catalog.hint`: "Olusturulacak acilari secin. Her aci icin bir gorsel."
   - `workspace.catalog.noProductImage`: "Lutfen bir urun gorseli yukleyin"
   - `workspace.catalog.noAngles`: "En az bir aci secin"
   - `workspace.colorway.productImage`: "Urun Gorseli"
   - `workspace.colorway.colors`: "Renkler"
   - `workspace.colorway.addColor`: "Ekle"
   - `workspace.colorway.colorPlaceholder`: "ornegin: Lacivert, #FF0000"
   - `workspace.colorway.hint`: "Eklenecek renkler. Her renk icin bir varyasyon."
   - `workspace.colorway.noProductImage`: "Lutfen bir urun gorseli yukleyin"
   - `workspace.colorway.noColors`: "En az bir renk ekleyin"
   - `workspace.colorway.maxColors`: "En fazla 8 renk"

4. Add matching English keys to `en.ts`:

   - `workspace.catalog.productImage`: "Product Image"
   - `workspace.catalog.angles`: "Angles"
   - `workspace.catalog.angleFront`: "Front"
   - `workspace.catalog.angleBack`: "Back"
   - `workspace.catalog.angleSideLeft`: "Side Left"
   - `workspace.catalog.angleSideRight`: "Side Right"
   - `workspace.catalog.angle34Front`: "3/4 Front"
   - `workspace.catalog.angle34Back`: "3/4 Back"
   - `workspace.catalog.hint`: "Select angles to generate. One image per angle."
   - `workspace.catalog.noProductImage`: "Please upload a product image"
   - `workspace.catalog.noAngles`: "Select at least one angle"
   - `workspace.colorway.productImage`: "Product Image"
   - `workspace.colorway.colors`: "Colors"
   - `workspace.colorway.addColor`: "Add"
   - `workspace.colorway.colorPlaceholder`: "e.g. Navy Blue, #FF0000"
   - `workspace.colorway.hint`: "Add colors to generate. One image per color variation."
   - `workspace.colorway.noProductImage`: "Please upload a product image"
   - `workspace.colorway.noColors`: "Add at least one color"
   - `workspace.colorway.maxColors`: "Maximum 8 colors"
  </action>
  <verify>
    <automated>cd "C:/Users/mertb/texora.studio" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx has catalog and colorway modes with enabled: true
    - Sidebar.tsx imports and conditionally renders CatalogPanel and ColorwayPanel
    - GenerationControls.tsx imports generateCatalog and generateColorway
    - GenerationControls.tsx contains "catalog" mode handling with catalogAngles loop
    - GenerationControls.tsx contains "colorway" mode handling with colorwayColors loop
    - tr.ts contains "workspace.catalog.angles" and "workspace.colorway.colors"
    - en.ts contains "workspace.catalog.angles" and "workspace.colorway.colors"
    - npm run build succeeds with exit code 0
  </acceptance_criteria>
  <done>Catalog and Colorway modes fully enabled in sidebar with proper panels, generation routing handles multi-image output, all text translated</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `grep -r "enabled: true" src/components/Sidebar.tsx` shows catalog and colorway are enabled
3. `grep -r "generateCatalog" src/lib/fal.ts` shows the function exists
4. `grep -r "generateColorway" src/lib/fal.ts` shows the function exists
5. `grep -r "CatalogPanel" src/components/Sidebar.tsx` shows conditional render
6. `grep -r "ColorwayPanel" src/components/Sidebar.tsx` shows conditional render
7. `grep -r "catalogAngles" src/components/GenerationControls.tsx` shows routing
</verification>

<success_criteria>
- Catalog mode is selectable in sidebar navigation
- Catalog mode shows product image upload and angle checkboxes
- Generating in Catalog mode creates one image per selected angle
- Colorway mode is selectable in sidebar navigation
- Colorway mode shows product image upload and color list
- Generating in Colorway mode creates one image per color
- All generation results appear in the grid
- All UI text translated in Turkish and English
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-full-generation-suite-organization/03-04-SUMMARY.md`
</output>
