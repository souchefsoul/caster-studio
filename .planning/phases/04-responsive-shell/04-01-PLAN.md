---
phase: 04-responsive-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/index.css
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-04

must_haves:
  truths:
    - "On mobile Safari, CSS env(safe-area-inset-*) returns nonzero values on notched devices"
    - "No element on any page produces horizontal scroll at 320px–414px viewport widths"
    - "App renders at correct 1:1 scale on phone without pinch-to-zoom"
  artifacts:
    - path: "index.html"
      provides: "Viewport meta tag with viewport-fit=cover"
      contains: "viewport-fit=cover"
    - path: "src/index.css"
      provides: "Global overflow-x hygiene on html/body"
      contains: "overflow-x"
  key_links:
    - from: "index.html <meta viewport>"
      to: "iOS Safari safe-area-inset calculation"
      via: "viewport-fit=cover attribute"
      pattern: "viewport-fit=cover"
    - from: "src/index.css html, body rule"
      to: "Browser horizontal scroll prevention"
      via: "overflow-x: hidden"
      pattern: "overflow-x:\\s*hidden"
---

<objective>
Fix the document-level foundation that every downstream responsive behavior depends on: the viewport meta tag must declare `viewport-fit=cover` so iOS yields nonzero safe-area insets, and `html`/`body` must have `overflow-x: hidden` as a site-wide safety net against any stray horizontal scroll.

Purpose: Without `viewport-fit=cover`, Plan 04-02's `env(safe-area-inset-*)` padding reads as 0 on iPhone — the whole safe-area strategy silently no-ops. Without global `overflow-x: hidden`, one overflowing flex child anywhere in the tree breaks the 360px rendering for the entire app. Both are shell-level primitives that must land before any component-level work.

Output: Updated `index.html` viewport meta and a base `@layer base` rule in `src/index.css` applying `overflow-x: hidden` to `html, body`.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-responsive-shell/04-CONTEXT.md

@index.html
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update viewport meta tag with viewport-fit=cover</name>
  <files>index.html</files>
  <read_first>
    - index.html (the file being modified)
    - .planning/phases/04-responsive-shell/04-CONTEXT.md (decisions authority — Safe-Area Insets section)
  </read_first>
  <action>
    In `index.html`, replace the existing viewport meta line:

      <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    with exactly:

      <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />

    Do NOT add any other meta tags (no theme-color, no mobile-web-app-capable, no PWA manifest). Those are out of scope per REQUIREMENTS.md v1.2+.

    Do NOT change anything else in the file (favicon link, title, root div, script tag all remain exactly as-is).
  </action>
  <verify>
    <automated>grep -c 'viewport-fit=cover' index.html</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains the exact string `viewport-fit=cover`
    - `index.html` contains the exact string `width=device-width, initial-scale=1, viewport-fit=cover` on the meta viewport line
    - `index.html` has exactly ONE `<meta name="viewport"` line (no duplicates introduced)
    - `grep -c 'meta name="viewport"' index.html` returns `1`
    - No other lines in `index.html` changed (favicon, title, root div, script src unchanged)
  </acceptance_criteria>
  <done>Viewport meta includes viewport-fit=cover; iOS Safari will now yield nonzero env(safe-area-inset-*) values on notched devices.</done>
</task>

<task type="auto">
  <name>Task 2: Add global overflow-x hygiene to html and body</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css (the file being modified)
    - .planning/phases/04-responsive-shell/04-CONTEXT.md (Overflow & Scroll Hygiene decisions section)
  </read_first>
  <action>
    Edit `src/index.css`. Locate the existing `@layer base` block (currently at lines 120–130, containing the `*`, `body`, and `html` rules).

    Modify the `html` rule inside `@layer base` so it looks like this:

      html {
        @apply font-sans;
        overflow-x: hidden;
      }

    Modify the `body` rule inside `@layer base` so it looks like this:

      body {
        @apply bg-background text-foreground;
        overflow-x: hidden;
      }

    Do NOT remove or modify the `*` selector rule. Do NOT change anything outside `@layer base`. Do NOT touch `:root`, `.dark`, `@theme inline`, `@layer utilities`, or the `@import` statements.

    Use raw CSS `overflow-x: hidden` (NOT `@apply overflow-x-hidden`) because Tailwind v4 may not expose that utility in `@apply` context depending on resolution order; raw CSS is unambiguous.

    Rationale (from CONTEXT): this is a safety net — component-level fixes in 04-02 and 04-03 address root causes (sidebar width, toolbar flex-wrap). This rule catches anything those miss so 360px viewports never horizontally scroll.
  </action>
  <verify>
    <automated>grep -c 'overflow-x:\s*hidden' src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contains `overflow-x: hidden` inside the `html` rule within `@layer base`
    - `src/index.css` contains `overflow-x: hidden` inside the `body` rule within `@layer base`
    - `grep -c 'overflow-x' src/index.css` returns at least `2`
    - `@layer base` block still contains the original `*` rule with `@apply border-border outline-ring/50`
    - `@apply font-sans` still present on `html`
    - `@apply bg-background text-foreground` still present on `body`
    - `:root`, `.dark`, `@theme inline`, `@layer utilities`, and all `@import` lines unchanged
    - `npm run build` exits with code `0`
  </acceptance_criteria>
  <done>html and body have overflow-x: hidden applied globally; any stray horizontal overflow from downstream components is clipped at the document root.</done>
</task>

</tasks>

<verification>
- `index.html` contains `viewport-fit=cover` (grep match)
- `src/index.css` contains `overflow-x: hidden` on both `html` and `body` rules inside `@layer base`
- `npm run build` succeeds (TypeScript + Vite)
- Manual: open dev server on phone emulator at 360px width — page renders without pinch-zoom prompt and no horizontal scrollbar appears on the document
</verification>

<success_criteria>
1. Viewport meta includes `viewport-fit=cover` exactly once in `index.html`
2. Both `html` and `body` have `overflow-x: hidden` in `@layer base`
3. `npm run build` exits 0
4. No regressions: the app still loads in the browser and renders correctly at desktop widths
</success_criteria>

<output>
After completion, create `.planning/phases/04-responsive-shell/04-01-SUMMARY.md` documenting:
- Files modified (index.html, src/index.css)
- Exact meta tag value applied
- Exact CSS rules added
- Build pass confirmation
- Note: This is foundational — Plans 04-02 and 04-03 depend on `viewport-fit=cover` for safe-area insets to function.
</output>
