---
phase: 06-mode-panels-on-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ImageUpload.tsx
  - src/components/OnModelPanel.tsx
  - src/lib/translations/tr.ts
  - src/lib/translations/en.ts
autonomous: true
requirements: [MODES-01]
must_haves:
  truths:
    - "On mobile (<lg), user can upload multiple product photos through the On-Model grid; every slot is at least 80x80px"
    - "On mobile (<lg), user can tap 'Önden' or 'Arkadan' and the selected view state toggles"
    - "Every file input opens the native OS image picker (no app-enforced mime subset restricting the sheet)"
    - "All On-Model hardcoded Turkish strings are localized through workspace.onModel.* keys in tr.ts and en.ts"
    - "Phase 6 i18n keys (workspace.onModel.viewQuestion, workspace.onModel.viewFront, workspace.onModel.viewBack, workspace.onModel.hint, workspace.catalog.anglesLabel, workspace.catalog.hintGenerated, workspace.video.uploadFromDevice, workspace.video.pickFromGallery) exist in both tr.ts and en.ts"
  artifacts:
    - path: "src/components/ImageUpload.tsx"
      provides: "Shared upload control with accept='image/*' default"
      contains: "accept = 'image/*'"
    - path: "src/components/OnModelPanel.tsx"
      provides: "3-col multi-image upload grid with 80x80 min slot + tap-sized Front/Back toggle"
      contains: "grid-cols-3"
      contains_also: "min-h-20"
    - path: "src/lib/translations/tr.ts"
      provides: "Turkish translations with new workspace.* keys used across Phase 6 panels"
      contains: "viewQuestion:"
    - path: "src/lib/translations/en.ts"
      provides: "English translations (parallel to tr.ts)"
      contains: "viewQuestion:"
  key_links:
    - from: "src/components/OnModelPanel.tsx"
      to: "src/lib/translations/tr.ts#workspace.onModel"
      via: "t('workspace.onModel.viewQuestion' | 'viewFront' | 'viewBack')"
      pattern: "t\\('workspace\\.onModel\\.(viewQuestion|viewFront|viewBack|hint)"
    - from: "src/components/OnModelPanel.tsx"
      to: "src/components/ImageUpload.tsx"
      via: "Both components share the 'accept=image/*' default; OnModelPanel keeps its own hidden input and MUST set accept=image/* on it"
      pattern: "accept=\\{?['\"]image/\\*['\"]"
---

<objective>
Lock in the Phase 6 shared foundation: upload file inputs accept all image types (no mime subset), the On-Model multi-image grid meets the 80x80px minimum on mobile, the Front/Back view toggle has tap-sized targets and localized labels, and every new translation key that later Phase 6 plans will consume exists in both tr.ts and en.ts.

Purpose: Every downstream Phase 6 plan (Catalog, Colorway, Design Copy, Video, Brand Face) either reads from ImageUpload or reads new i18n keys. Adding them in one foundation plan means Plans 06-02..06-06 never race each other on tr.ts/en.ts and can run in parallel in Wave 2.

Output: Updated ImageUpload (accept="image/*"), updated OnModelPanel (mobile-ready grid + buttons + i18n), fully localized translation files.
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mode-panels-on-mobile/06-CONTEXT.md

<!-- Upstream foundation -->
@.planning/phases/05.1-mobile-layout-flip/05.1-02-SUMMARY.md

<!-- Files this plan modifies -->
@src/components/ImageUpload.tsx
@src/components/OnModelPanel.tsx
@src/lib/translations/tr.ts
@src/lib/translations/en.ts

<interfaces>
<!-- ImageUpload props (src/components/ImageUpload.tsx — do not break these; tasks below keep them unchanged) -->
```typescript
interface ImageUploadProps {
  value: string | null
  onChange: (dataUrl: string | null) => void
  label?: string
  accept?: string   // default changes from 'image/jpeg,image/png,image/webp' to 'image/*'
}
```

<!-- OnModelPanel already uses its own hidden file input (not ImageUpload); that input MUST also switch to accept="image/*". Current value at ~line 93: accept="image/jpeg,image/png,image/webp" — change to accept="image/*". -->

<!-- Turkish strings currently hardcoded inside OnModelPanel.tsx that MUST be moved to i18n: -->
<!--   "Yalnızca çıktı fotoğrafta görülmesi istenilen tarafı yükleyin."  (line 57-58)   → workspace.onModel.hint -->
<!--   "Modeli hangi taraftan görelim?"  (line 103)                                     → workspace.onModel.viewQuestion -->
<!--   "Önden"  (line 112)                                                              → workspace.onModel.viewFront -->
<!--   "Arkadan"  (line 119)                                                            → workspace.onModel.viewBack -->

<!-- Turkish strings currently hardcoded inside CatalogPanel.tsx (Plan 06-02 will localize — keys added here): -->
<!--   "Açılar" (line 51)                                  → workspace.catalog.anglesLabel -->
<!--   "Her seçilen açı için bir görsel üretilir." (line 66) → workspace.catalog.hintGenerated -->
<!--   Angle labels ("Önden", "Arkadan", "Sol Yan", "Sağ Yan", "3/4 Ön", "3/4 Arka") are left as-is here; Plan 06-02 will switch AVAILABLE_ANGLES to use existing workspace.catalog.angleFront/angleBack/angleSideLeft/angleSideRight/angle34Front/angle34Back keys that already exist in tr.ts/en.ts. -->

<!-- New keys Plan 06-05 (Video) needs; added here so Plan 06-05 only touches VideoPanel.tsx: -->
<!--   workspace.video.uploadFromDevice: Turkish "Cihazdan Yükle" / English "Upload from Device" -->
<!--   workspace.video.pickFromGallery:  Turkish "Galeriden Seç"  / English "Pick from Gallery" -->
<!--   (Note: workspace.video.selectFromGallery already exists — keep it; pickFromGallery is the short button label.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 6 i18n keys to tr.ts and en.ts (OnModel + Catalog + Video labels)</name>
  <files>src/lib/translations/tr.ts, src/lib/translations/en.ts</files>
  <read_first>src/lib/translations/tr.ts, src/lib/translations/en.ts</read_first>
  <action>
Add new keys to BOTH tr.ts and en.ts, preserving every existing key byte-for-byte.

1. In `src/lib/translations/tr.ts`, inside the `workspace.onModel` block (between `imagesHint:` and `addImage:`), add:
   - `hint: 'Yalnızca çıktı fotoğrafta görülmesi istenilen tarafı yükleyin.',`
   - `viewQuestion: 'Modeli hangi taraftan görelim?',`
   - `viewFront: 'Önden',`
   - `viewBack: 'Arkadan',`

2. In `src/lib/translations/tr.ts`, inside the `workspace.catalog` block, add (before `hint:`):
   - `anglesLabel: 'Açılar',`
   - `hintGenerated: 'Her seçilen açı için bir görsel üretilir.',`

3. In `src/lib/translations/tr.ts`, inside the `workspace.video` block, add (before `duration:`):
   - `uploadFromDevice: 'Cihazdan Yükle',`
   - `pickFromGallery: 'Galeriden Seç',`

4. In `src/lib/translations/en.ts`, mirror each addition at the exact same nesting path:
   - `workspace.onModel.hint: 'Upload only the side you want to see in the output photo.'`
   - `workspace.onModel.viewQuestion: 'Which view of the model?'`
   - `workspace.onModel.viewFront: 'Front'`
   - `workspace.onModel.viewBack: 'Back'`
   - `workspace.catalog.anglesLabel: 'Angles'`
   - `workspace.catalog.hintGenerated: 'One image will be generated per selected angle.'`
   - `workspace.video.uploadFromDevice: 'Upload from Device'`
   - `workspace.video.pickFromGallery: 'Pick from Gallery'`

Every other key in both files must remain byte-identical. Do NOT reorder or remove existing keys. Preserve the `as const` suffix on tr.ts and the `: TranslationKeys` type assertion on en.ts.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -40</automated>
    (Build must exit 0 — TypeScript will fail if tr.ts and en.ts diverge because en.ts is typed as `TranslationKeys = DeepStringify&lt;typeof tr&gt;`.)
  </verify>
  <acceptance_criteria>
- `grep -n "viewQuestion: 'Modeli hangi taraftan" src/lib/translations/tr.ts` returns a match
- `grep -n "viewQuestion: 'Which view of the model" src/lib/translations/en.ts` returns a match
- `grep -n "viewFront: 'Önden'" src/lib/translations/tr.ts` returns a match
- `grep -n "viewBack: 'Arkadan'" src/lib/translations/tr.ts` returns a match
- `grep -n "viewFront: 'Front'" src/lib/translations/en.ts` returns a match
- `grep -n "viewBack: 'Back'" src/lib/translations/en.ts` returns a match
- `grep -n "hint: 'Yalnızca çıktı fotoğrafta" src/lib/translations/tr.ts` returns a match (nested under onModel)
- `grep -n "anglesLabel: 'Açılar'" src/lib/translations/tr.ts` returns a match
- `grep -n "anglesLabel: 'Angles'" src/lib/translations/en.ts` returns a match
- `grep -n "hintGenerated: 'Her seçilen açı" src/lib/translations/tr.ts` returns a match
- `grep -n "hintGenerated: 'One image will be generated" src/lib/translations/en.ts` returns a match
- `grep -n "uploadFromDevice: 'Cihazdan Yükle'" src/lib/translations/tr.ts` returns a match
- `grep -n "uploadFromDevice: 'Upload from Device'" src/lib/translations/en.ts` returns a match
- `grep -n "pickFromGallery: 'Galeriden Seç'" src/lib/translations/tr.ts` returns a match
- `grep -n "pickFromGallery: 'Pick from Gallery'" src/lib/translations/en.ts` returns a match
- `npm run build` exits 0 (proves tr/en stay structurally identical — TranslationKeys enforces this)
  </acceptance_criteria>
  <done>Eight new keys exist in both tr.ts and en.ts at the specified nesting paths, and `npm run build` passes without TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Switch ImageUpload default accept to image/* and remove hardcoded mime subset</name>
  <files>src/components/ImageUpload.tsx</files>
  <read_first>src/components/ImageUpload.tsx</read_first>
  <action>
In `src/components/ImageUpload.tsx`:

1. Change the destructured default for the `accept` prop:
   - From: `accept = 'image/jpeg,image/png,image/webp',`
   - To:   `accept = 'image/*',`

Do NOT touch any other line. Do NOT add a `capture` attribute (Phase 7 TOUCH-03 owns capture hints — this plan is only widening the accept mime filter per CONTEXT's locked decision).

Rationale: iOS and Android natively offer camera + photo library + files when `accept="image/*"` — users pick the source from the OS sheet. Restricting to jpeg/png/webp strips HEIC (iOS default) and triggers confusing "no compatible files" messages on some devices.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "accept = 'image/\\*'" src/components/ImageUpload.tsx` returns exactly 1 match
- `grep -n "image/jpeg,image/png,image/webp" src/components/ImageUpload.tsx` returns 0 matches
- `grep -n "capture=" src/components/ImageUpload.tsx` returns 0 matches (no capture attribute added — Phase 7 work)
- `npm run build` exits 0
  </acceptance_criteria>
  <done>ImageUpload's default accept is `image/*`; no mime-subset whitelist remains; no capture hint added.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: On-Model panel — 3-col grid with 80x80 min slot + tap-sized localized Front/Back toggle + accept=image/*</name>
  <files>src/components/OnModelPanel.tsx</files>
  <read_first>src/components/OnModelPanel.tsx, src/lib/translations/tr.ts</read_first>
  <action>
Edit `src/components/OnModelPanel.tsx`. Apply ALL of the following, and ONLY the following:

1. **Replace the hardcoded Turkish hint paragraph.**
   Current (around lines 56–58):
   ```tsx
   <p className="text-xs text-muted-foreground">
     Yalnızca çıktı fotoğrafta görülmesi istenilen tarafı yükleyin.
   </p>
   ```
   Replace with:
   ```tsx
   <p className="text-xs text-muted-foreground">
     {t('workspace.onModel.hint')}
   </p>
   ```

2. **Enforce 80x80px minimum slot size on mobile (MODES-01 non-negotiable).**
   The current grid wrapper (around line 63) is:
   ```tsx
   <div
     onDragOver={(e) => e.preventDefault()}
     onDrop={handleDrop}
     className="grid grid-cols-3 gap-2"
   >
   ```
   Keep `grid-cols-3 gap-2`. The slot size is governed by the child `aspect-square` on each tile — at 3 columns on a 360px-wide sidebar (after Phase 5.1 made it full-width), each slot is roughly `(360 - 2*gap - 2*panel-padding) / 3` ≈ 108px, which already exceeds 80px. To make the 80px floor an explicit invariant (not a viewport-width accident), add `min-h-20 min-w-20` to each tile. Update BOTH tile elements (the existing image tile AND the empty upload tile) so the floor applies even when the viewport is narrower than 360px:

   a. Image tile (around line 66): change
      `className="relative aspect-square border border-border bg-muted/50"`
      to
      `className="relative aspect-square min-h-20 min-w-20 border border-border bg-muted/50"`

   b. Empty upload tile (around lines 78–82): change
      `className="flex aspect-square flex-col items-center justify-center gap-1 border border-dashed border-border bg-muted/50 hover:border-primary"`
      to
      `className="flex aspect-square min-h-20 min-w-20 flex-col items-center justify-center gap-1 border border-dashed border-border bg-muted/50 hover:border-primary"`

3. **Widen the file input's accept to image/* (CONTEXT locked).**
   Around line 93:
   - From: `accept="image/jpeg,image/png,image/webp"`
   - To:   `accept="image/*"`

4. **Localize the "Modeli hangi taraftan görelim?" label.**
   Around line 103:
   - From: `<p className="text-xs font-medium">Modeli hangi taraftan görelim?</p>`
   - To:   `<p className="text-xs font-medium">{t('workspace.onModel.viewQuestion')}</p>`

5. **Localize the Front/Back buttons AND enforce a tap-friendly minimum height (still within Phase 6 scope — CONTEXT says "honor 40px tap where natural").**
   The current block (around lines 104–121) uses `size="xs"`. Change both buttons' className from `"rounded-none"` to `"rounded-none min-h-10"` and replace the visible text:

   - "Önden" → `{t('workspace.onModel.viewFront')}`
   - "Arkadan" → `{t('workspace.onModel.viewBack')}`

   Final JSX for the Front/Back grid should read exactly:
   ```tsx
   <div className="grid grid-cols-2 gap-1">
     <Button
       variant={onModelView === 'front' ? 'default' : 'outline'}
       size="xs"
       onClick={() => setOnModelView('front')}
       className="rounded-none min-h-10"
     >
       {t('workspace.onModel.viewFront')}
     </Button>
     <Button
       variant={onModelView === 'back' ? 'default' : 'outline'}
       size="xs"
       onClick={() => setOnModelView('back')}
       className="rounded-none min-h-10"
     >
       {t('workspace.onModel.viewBack')}
     </Button>
   </div>
   ```

Do NOT change: MAX_IMAGES constant, processFiles/handleChange/handleDrop logic, error state, the `grid grid-cols-3 gap-2` base classes on the grid wrapper (3-col on all viewports is correct per CONTEXT for On-Model), or anything outside the scope above.

Desktop is intentionally unaffected: min-h-20/min-w-20 is a FLOOR — on lg+ the aspect-square tiles already exceed 80px, so the constraint is a no-op above mobile widths.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -20 &amp;&amp; npm run lint 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "t('workspace.onModel.hint')" src/components/OnModelPanel.tsx` returns exactly 1 match
- `grep -n "Yalnızca çıktı fotoğrafta" src/components/OnModelPanel.tsx` returns 0 matches (hardcoded Turkish removed)
- `grep -n "t('workspace.onModel.viewQuestion')" src/components/OnModelPanel.tsx` returns exactly 1 match
- `grep -n "Modeli hangi taraftan" src/components/OnModelPanel.tsx` returns 0 matches
- `grep -n "t('workspace.onModel.viewFront')" src/components/OnModelPanel.tsx` returns exactly 1 match
- `grep -n "t('workspace.onModel.viewBack')" src/components/OnModelPanel.tsx` returns exactly 1 match
- `grep -cE "(>Önden<|>Arkadan<)" src/components/OnModelPanel.tsx` returns 0 (literal Turkish strings no longer in JSX)
- `grep -c "aspect-square min-h-20 min-w-20" src/components/OnModelPanel.tsx` returns at least 2 (both tiles)
- `grep -n 'accept="image/\\*"' src/components/OnModelPanel.tsx` returns exactly 1 match
- `grep -n "image/jpeg,image/png,image/webp" src/components/OnModelPanel.tsx` returns 0 matches
- `grep -cE 'className="rounded-none min-h-10"' src/components/OnModelPanel.tsx` returns at least 2 (both Front/Back buttons)
- `grep -n "grid grid-cols-3 gap-2" src/components/OnModelPanel.tsx` returns exactly 1 match (3-col grid preserved on all viewports per MODES-01)
- `npm run build` exits 0
- `npm run lint` exits 0
  </acceptance_criteria>
  <done>On-Model panel renders with localized labels, every upload slot is at least 80x80px on mobile, Front/Back buttons are 40px-tall tap targets, and the file input accepts any image MIME so iOS/Android offer camera+library+files from the native sheet.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (TypeScript enforces tr/en structural parity via TranslationKeys type)
- `npm run lint` exits 0
- Grep spot-checks from each task's acceptance_criteria all pass
- Manual sanity (optional, not required for this plan to be considered complete): at 360px viewport the OnModelPanel shows 3 square slots per row each ≥ 80px tall; Front/Back buttons are ≥ 40px tall; the file input opens the native OS image picker offering camera, library, and files
</verification>

<success_criteria>
- MODES-01 satisfied: multi-image grid is 3-col with 80x80 min slots on mobile; Front/Back toggle is tap-sized and localized; file input opens native picker via accept="image/*"
- All i18n keys needed by Plans 06-02..06-05 are present in both tr.ts and en.ts (no Wave-2 plan will touch translations files — they can run in parallel)
- Zero desktop regression: `grid-cols-3` is unchanged (On-Model was already 3-col on desktop); min-h/min-w-20 are no-ops above mobile widths; the Button `min-h-10` only enlarges the default xs-size below its normal desktop footprint, matching shadcn button-lg behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-mode-panels-on-mobile/06-01-SUMMARY.md` describing the i18n keys added, ImageUpload accept change, and OnModel reflow.
</output>
