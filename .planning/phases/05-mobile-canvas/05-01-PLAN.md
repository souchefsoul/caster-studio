---
phase: 05-mobile-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Canvas.tsx
autonomous: true
requirements:
  - CANVAS-01
  - CANVAS-03
  - CANVAS-04
must_haves:
  truths:
    - "On phone (<lg), every grid item's delete/download/create-video overlay is visible without any hover interaction"
    - "On desktop (lg+), grid item overlay remains hidden by default and appears on hover (no desktop regression)"
    - "Grid column count is 2 on phone, 3 on md tablet, 4 on lg+ desktop"
    - "Canvas toolbar reflows cleanly at 360px with no overlapping or cut-off controls (Phase 4 work preserved)"
  artifacts:
    - path: "src/components/Canvas.tsx"
      provides: "Mobile-touch-reachable grid overlay + verified responsive grid columns + preserved responsive toolbar"
      contains: "lg:opacity-0 lg:group-hover:opacity-100"
  key_links:
    - from: "Grid overlay div"
      to: "Always-visible state below lg breakpoint"
      via: "Tailwind responsive modifier flip: opacity-100 lg:opacity-0 lg:group-hover:opacity-100"
      pattern: "opacity-100 lg:opacity-0 lg:group-hover:opacity-100"
---

<objective>
Make the canvas grid's per-item action overlay (delete / download / create-video) reachable on touch devices by flipping its visibility from hover-only to always-visible below the `lg` breakpoint, while preserving hover-reveal behavior on desktop. Also verify CANVAS-01 (responsive grid columns) and CANVAS-03 (toolbar reflow) are satisfied — both are expected to already be in place from Phase 4 and the existing grid structure, so this plan's role is a regression lock-in with grep-verifiable acceptance criteria.

Purpose: Touch users currently have no way to delete, download, or create-video from a grid item because the overlay only appears on hover. This breaks feature parity on phones. CANVAS-01 and CANVAS-03 are already coded; we capture them in `requirements:` and verify they do not regress.

Output: `src/components/Canvas.tsx` with the grid overlay always-visible on mobile/tablet and hover-gated on desktop; grid column structure and toolbar reflow unchanged from current (verified by grep).
</objective>

<execution_context>
@C:/Users/mertb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mertb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-canvas/05-CONTEXT.md
@src/components/Canvas.tsx

<interfaces>
<!-- Key patterns in Canvas.tsx the executor needs to know -->
<!-- Lines are current-state snapshots; the executor should re-verify before editing -->

Grid container (~line 162):
```tsx
<div className="grid grid-cols-2 gap-2 md:grid-cols-3 lg:grid-cols-4">
```

Grid item hover overlay (~line 184) — the line being changed:
```tsx
<div className="absolute bottom-0 left-0 right-0 flex items-center justify-between bg-black/70 px-2 py-1.5 opacity-0 transition-opacity group-hover:opacity-100">
```

Toolbar (~line 46) — from Phase 4, MUST remain unchanged:
```tsx
<div className="flex items-center justify-between gap-2 border-b border-border px-4 py-2 pt-[max(0.5rem,env(safe-area-inset-top))]">
```

Inner flex-wrap toolbar row (~line 47):
```tsx
<div className="flex min-w-0 items-center gap-1 flex-wrap">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip grid overlay visibility — always-visible on mobile, hover-gated on lg+</name>
  <files>src/components/Canvas.tsx</files>
  <read_first>
    - src/components/Canvas.tsx (the file being modified)
    - .planning/phases/05-mobile-canvas/05-CONTEXT.md (authoritative user decisions for CANVAS-04)
  </read_first>
  <action>
    Locate the grid item overlay div (currently near line 184). Its current className is exactly:

    `"absolute bottom-0 left-0 right-0 flex items-center justify-between bg-black/70 px-2 py-1.5 opacity-0 transition-opacity group-hover:opacity-100"`

    Replace `opacity-0 transition-opacity group-hover:opacity-100` with:

    `opacity-100 transition-opacity lg:opacity-0 lg:group-hover:opacity-100`

    So the new full className becomes exactly:

    `"absolute bottom-0 left-0 right-0 flex items-center justify-between bg-black/70 px-2 py-1.5 opacity-100 transition-opacity lg:opacity-0 lg:group-hover:opacity-100"`

    Do NOT change any other classes on this element (keep `absolute bottom-0 left-0 right-0 flex items-center justify-between bg-black/70 px-2 py-1.5 transition-opacity`).
    Do NOT touch the inner `<button>` / `<a>` children — their handlers and styling stay identical.
    Do NOT redesign the overlay visual (no bigger buttons, no different bg color, no new layout).
  </action>
  <verify>
    <automated>grep -c 'opacity-100 transition-opacity lg:opacity-0 lg:group-hover:opacity-100' src/components/Canvas.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'opacity-100 transition-opacity lg:opacity-0 lg:group-hover:opacity-100' src/components/Canvas.tsx` returns 1
    - `grep -c 'lg:opacity-0' src/components/Canvas.tsx` returns 1
    - `grep -c 'lg:group-hover:opacity-100' src/components/Canvas.tsx` returns 1
    - `grep -c 'opacity-0 transition-opacity group-hover:opacity-100' src/components/Canvas.tsx` returns 0 (old pattern gone)
    - `src/components/Canvas.tsx` still contains `bg-black/70 px-2 py-1.5` (overlay visual unchanged)
    - `src/components/Canvas.tsx` still contains `e.stopPropagation()` at least twice (overlay buttons still stop propagation)
  </acceptance_criteria>
  <done>Grid item overlay is always-visible below `lg` breakpoint and hover-gated at `lg+`; no other overlay styling or behavior has changed.</done>
</task>

<task type="auto">
  <name>Task 2: Regression lock — verify CANVAS-01 grid columns + CANVAS-03 toolbar reflow unchanged</name>
  <files>src/components/Canvas.tsx</files>
  <read_first>
    - src/components/Canvas.tsx (the file being verified)
    - .planning/phases/05-mobile-canvas/05-CONTEXT.md (CANVAS-01 + CANVAS-03 already-satisfied notes)
  </read_first>
  <action>
    No code changes in this task. This is a verification-only step whose purpose is to lock in CANVAS-01 and CANVAS-03 coverage via grep-verifiable regression checks.

    Confirm the grid container still uses exactly the responsive column pattern established earlier:
    - Grid columns: `grid grid-cols-2 gap-2 md:grid-cols-3 lg:grid-cols-4` (all four tokens on the same element)

    Confirm the canvas toolbar still has the Phase 4 wrapping + safe-area classes:
    - Outer toolbar row: `pt-[max(0.5rem,env(safe-area-inset-top))]` present
    - Inner toolbar row still has `flex-wrap` AND `gap-2` (on either the outer or inner flex row) AND `min-w-0`

    If any of these regressed, restore them to match the current code shown in the interfaces block above. Do not add new classes; just restore.

    Run `npm run build` to confirm the Task 1 change compiles cleanly under Vite + Tailwind v4.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'grid-cols-2' src/components/Canvas.tsx` returns at least 1
    - `grep -c 'md:grid-cols-3' src/components/Canvas.tsx` returns at least 1
    - `grep -c 'lg:grid-cols-4' src/components/Canvas.tsx` returns at least 1
    - `grep -c 'gap-2' src/components/Canvas.tsx` returns at least 1
    - `grep -c 'pt-\[max(0.5rem,env(safe-area-inset-top))\]' src/components/Canvas.tsx` returns at least 1 (safe-area from Phase 4)
    - `grep -c 'flex-wrap' src/components/Canvas.tsx` returns at least 1
    - `grep -c 'min-w-0' src/components/Canvas.tsx` returns at least 1
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>CANVAS-01 (responsive columns) and CANVAS-03 (toolbar reflow) are verified still in place; build passes; plan 05-01 contains no regressions from Phase 4.</done>
</task>

</tasks>

<verification>
Manual smoke (not required for automated gate, noted for human spot-check):
- Open the app in Chrome DevTools at 360×640 iPhone SE viewport. Grid items should show the dark overlay strip with delete / create-video / download text all the time (no hover needed).
- Resize to ≥1024px width. Overlay should disappear by default and reappear on hover.
- Toolbar at 360px should not overflow horizontally; all controls should be present (may wrap to 2 rows).
</verification>

<success_criteria>
- Task 1 grep pattern `opacity-100 transition-opacity lg:opacity-0 lg:group-hover:opacity-100` appears exactly once in Canvas.tsx
- Task 2 regression greps all pass
- `npm run build` exits 0
- No changes to any file other than `src/components/Canvas.tsx`
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-canvas/05-01-SUMMARY.md` following the summary template, noting:
- Exact grep pattern used for the overlay flip
- Confirmation that CANVAS-01 and CANVAS-03 required no code changes (Phase 4 + existing structure already satisfied them)
</output>
